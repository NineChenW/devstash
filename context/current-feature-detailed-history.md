# Current Feature - Detailed History

This file contains the complete, detailed history records moved from `current-feature.md` for archival purposes.

## Detailed History Records

- **2026-05-19**: Test Feature for History Generation - Test goal 1: Verify script extracts feature name correctly; Test goal 2: Ensure goals are captured and included in detailed record; Test goal 3: Confirm notes are processed and added to history. Note 1: This is a test feature to validate the history generation script; Note 2: Testing that both goals and notes are properly combined in detailed record
- **2026-04-14**: Initial Next.js and Tailwind CSS v4 setup - Created project structure, coding standards, and AI interaction guidelines. Committed with message "chore: initial next.js and tailwind setup".
- **2026-04-15**: Dashboard UI Phase 1 implementation - Initialized ShadCN UI components, created dashboard route at /dashboard, implemented dark mode by default, added top bar with search and new item button, created sidebar and main area placeholders.
- **2026-04-16**: Dashboard UI Phase 2 implementation - Implemented collapsible sidebar with item types links, favorite collections, most recent collections, user avatar area, drawer icon for mobile/desktop toggle, fixed hydration error with mounted state, added localStorage persistence for sidebar state.
- **2026-04-16**: Dashboard UI Phase 2 completion - Created Sidebar component with collapsible functionality, SidebarDrawer for mobile view, integrated with dashboard page, added localStorage persistence for sidebar collapsed state. Build verified successfully.
- **2026-04-16**: Bug fixes and sidebar improvements - Fixed toggle icon not changing between collapsed/expanded states, added colored left border accent to PinnedItem to match CollectionCard, made Collections section in sidebar collapsible with chevron animation, fixed sidebar not rendering after browser back navigation (removed mounted guard from sidebar render).
- **2026-04-16**: Dashboard UI Phase 3 completion - Implemented stats cards (total items, collections, favorites), recent collections section, pinned items section, and recent items section. Created DashboardShell and StatsCard components, RecentItem component. Updated mock data for development. Build verified and merged to main.
- **2026-04-17**: Neon PostgreSQL + Prisma Setup - Configured Prisma 7 with Neon serverless adapter, created initial migration with all data models, added seed script with system item types and demo data, added database test script. Build verified and merged to main.
- **2026-04-17**: Seed Data Population - Overwrote seed file with spec-compliant data, created demo user (demo@devstash.io), seeded all 7 system item types, created 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources), populated collections with realistic items. Build verified and merged to main.
- **2026-04-17**: Dashboard Collections - Created src/lib/db/collections.ts with data fetching functions, replaced mock collection data in dashboard with real Neon database queries via Prisma, collection card border color derived from most-used content type, type icons shown per collection, stats fetched from database. Fixed pre-existing seed.ts type error. Build verified.
- **2026-04-17**: Dashboard Items - Created src/lib/db/items.ts with getPinnedItems and getRecentItems functions, replaced mock pinned/recent item data in dashboard with real Neon database queries via Prisma. Updated PinnedItem and RecentItem components to receive type icon/color/name directly (removed mock-data dependency) and added type tag pill. Pinned section hides when empty. Build verified.
- **2026-04-17**: Stats & Sidebar - Added getSystemItemTypesWithCounts to src/lib/db/items.ts returning system item types in fixed order (snippet, prompt, command, note, file, image, link) with per-user counts. Rewired Sidebar to consume real DB data via props (itemTypes, collections) threaded through DashboardShell and SidebarDrawer; removed mockItemTypes/mockCollections usage. System type links point to /items/[name]s. Favorites keep the star icon; recents show a colored dot using the collection's dominant item-type color. Added "View all collections" link to /collections. Build verified.
- **2026-04-18**: Add Pro Badge to Sidebar - Installed ShadCN Badge component at src/components/ui/badge.tsx. Added PRO_TYPES set (file, image) in Sidebar.tsx and rendered a small outlined PRO badge between the item-type title and the count for those types; count pinned right via ml-auto, badge hidden when sidebar is collapsed. Covers mobile drawer automatically since SidebarDrawer wraps Sidebar. Build verified and merged to main.
- **2026-04-18**: Codebase Quick Wins Cleanup - Applied low-risk fixes from the code-scanner audit. Extracted shared iconMap to src/lib/icon-map.ts (removed duplicates from Sidebar, CollectionCard, PinnedItem, RecentItem). Dropped unnecessary 'use client' from PinnedItem and RecentItem. Added take: 20 bound to getRecentCollections. Guarded JSON.parse in DashboardShell against malformed localStorage. Removed unused mock exports (kept mockUser until auth lands). Dropped redundant @@index([email]) and @@index([name]) via Prisma migration drop_redundant_indexes. Added dashboard loading.tsx skeleton and error.tsx boundary. Build verified and merged to main.
- **2026-04-19**: Auth Phase 1 - NextAuth + GitHub Provider - Installed next-auth@beta and @auth/prisma-adapter. Split config: src/auth.config.ts (edge-safe GitHub provider + jwt/session callbacks populating user.id) and src/auth.ts (PrismaAdapter + session strategy 'jwt'). Added /api/auth/[...nextauth] route handler re-exporting handlers.GET/POST. Created src/proxy.ts exporting `proxy = auth(...)` that redirects unauthenticated visitors to `/dashboard/*` to `/api/auth/signin?callbackUrl=...`. Extended Session.user with id via src/types/next-auth.d.ts. Build verified; /dashboard redirect confirmed via curl; default NextAuth sign-in page renders with GitHub button. Merged to main.
- **2026-04-20**: Auth Phase 2 - Credentials (Email/Password) Provider - Added Credentials provider alongside GitHub using the split-config pattern: src/auth.config.ts declares a Credentials placeholder with `authorize: () => null` (edge-safe); src/auth.ts filters the placeholder by provider id and re-registers Credentials with a bcryptjs-backed authorize that looks up the user via Prisma and compares against the hashed password. Added POST /api/auth/register route that validates name/email/password/confirmPassword, rejects duplicate emails (409), enforces min 8-char passwords, hashes with bcryptjs (cost 10), and returns {success, user}. No migration needed — password column already exists from the earlier add_user_password migration. Verified via curl: register happy-path (201), duplicate (409), mismatched pw (400), short pw (400); credentials signin sets session cookie and /dashboard returns 200; wrong password redirects to /signin?error=CredentialsSignin with null session; GitHub provider still listed at /api/auth/providers; unauth /dashboard still redirects. Build verified and merged to main.
- **2026-04-20**: Auth Phase 3 - Custom Sign-in / Register UI + Sidebar User - Added `pages: { signIn: "/sign-in" }` to src/auth.config.ts and updated src/proxy.ts to redirect unauthenticated `/dashboard/*` visitors to `/sign-in?callbackUrl=...`. Built custom `/sign-in` (credentials form + GitHub button via `signIn()`, honors `callbackUrl`, redirects signed-in users) and `/register` (name/email/password/confirm with client-side validation; posts to `/api/auth/register`; redirects to `/sign-in?registered=1`). Added `/profile` placeholder gated by `auth()`. Created reusable `UserAvatar` (image-or-initials fallback, cap 2 chars, `?` when empty) and `SidebarUser` client component with click-outside/Escape-close dropup containing a `signOut({ callbackUrl: "/sign-in" })` action; avatar wrapped in a Link to `/profile`. Threaded session data from `auth()` through DashboardShell → Sidebar / SidebarDrawer props; deleted src/lib/mock-data.ts. Added inline `GithubIcon` (lucide-react has no `Github` export in this version). Installed `sonner`, mounted `<Toaster>` in root layout, and fire a deduped success toast on `/sign-in` when `?registered=1` is present (then scrub the param from the URL). Kept `next.config.ts` `devIndicators: false`. Verified in-browser: sign-out clears session and lands on /sign-in; avatar → /profile works; register → /sign-in with toast → new user signs in and sees "P3" initials in sidebar. Build verified and merged to main.
- **2026-04-21**: Email Verification on Register (Resend) - Installed `resend` and built verification on top of the existing `VerificationToken` table (24h TTL; no migration). Added src/lib/email.ts (Resend client + branded HTML/text template; from-address and app URL driven by EMAIL_FROM / APP_URL / NEXT_PUBLIC_APP_URL / AUTH_URL) and src/lib/verification-token.ts (createVerificationToken replaces prior rows for the identifier via deleteMany then inserts a fresh 32-byte hex token; consumeVerificationToken returns `{ok,email}` or `{ok:false, reason:"not-found"|"expired"}` and always deletes the row). POST /api/auth/register now issues a token and sends the email after user creation inside a try/catch so send failures are logged but non-fatal. Added GET /api/auth/verify (consumes token, sets `User.emailVerified`, redirects to `/sign-in?verified=1` or `/sign-in?verify=expired|invalid|missing`) and POST /api/auth/verify/resend (uniform 200 response to avoid email enumeration; only actually sends if a matching unverified user exists). src/auth.ts adds an `EmailNotVerifiedError extends CredentialsSignin` (code `"EmailNotVerified"`) thrown in `authorize` when the password is valid but `emailVerified` is null; GitHub OAuth users are unaffected. SignInForm handles `res.code === "EmailNotVerified"` by showing a "Resend verification email" button; updated toasts for `?registered=1` (new copy prompts verification), `?verified=1`, and `?verify=expired|invalid|missing`. Also added scripts/cleanup-users.ts — deletes every user except demo@devstash.io (relying on User cascade for accounts/sessions/items/collections/custom itemTypes) plus stale VerificationToken rows, with a production-URL guard and interactive `yes` confirmation (skippable via `--yes`). Build verified and merged to main.
- **2026-04-21**: Toggle Email Verification Flag - Added a runtime kill-switch for the Resend verification flow, prompted by Resend sandbox mode only delivering to the account owner's own address. New `src/lib/features.ts` exports `isEmailVerificationEnabled()` which returns true only when `EMAIL_VERIFICATION_ENABLED` is `"true"`/`"1"` (case-insensitive, trimmed); any other value — including unset — disables verification. POST /api/auth/register now branches on the flag: when off, the user is created with `emailVerified: new Date()` and no token is issued / email sent; when on, behavior is identical to the prior feature. src/auth.ts gates the `EmailNotVerifiedError` throw behind the same flag so credentials sign-in succeeds for unverified users when off; GitHub OAuth is untouched. POST /api/auth/verify/resend short-circuits with a uniform 200 when off (preserves the enumeration-safe contract, skips the DB hit); GET /api/auth/verify is left functional so legacy tokens issued before the flip can still be consumed harmlessly. `/sign-in/page.tsx` now reads the flag server-side and threads it into `SignInForm` as `emailVerificationEnabled`; the `?registered=1` toast copy switches to "Account created — you can now sign in." when off so we don't promise an email that's not coming. The resend CTA remains naturally gated (only fires on `EmailNotVerified`, which can't happen when off). `.env.example` documents the new var with a "false" default (gitignored in this repo, so the file is edited locally only). Build verified and merged to main.
- **2026-04-21**: Forgot Password - Added a full password reset flow on top of the existing `VerificationToken` table (no migration) using a `pwreset:<email>` identifier namespace so reset tokens can coexist with email verification tokens in the same row set. Extended `src/lib/verification-token.ts` with `createPasswordResetToken` (1h TTL vs 24h for verification; deleteMany-then-insert replaces prior reset rows for the same email), `checkPasswordResetToken` (validates without consuming — used by the `/reset-password` server page to decide whether to render the form or an error state), and `consumePasswordResetToken` (single-use, delete-on-consume, returns `{ok,email}` or `{ok:false, reason:"not-found"|"expired"}`). Hardened `consumeVerificationToken` to skip any row whose identifier starts with `pwreset:` so GET `/api/auth/verify` can't accidentally invalidate a reset token — verified: passing a reset token to verify redirects to `?verify=invalid` and the DB row is untouched. Added `sendPasswordResetEmail` to `src/lib/email.ts` with a branded template linking to `/reset-password?token=...` and 1h expiry copy. POST `/api/auth/forgot-password` is enumeration-safe (uniform 200 regardless of match) and only issues a token + sends mail when the user exists _and_ has a password — GitHub-only accounts get the silent 200, no token, no email. POST `/api/auth/reset-password` validates the token, enforces 8-char minimum + match (same contract as register), bcrypt-hashes at cost 10, updates `User.password`, and deletes the token; returns `{error, code:"expired"|"invalid"|"missing"}` on failures so the client can link to `/forgot-password`. Not gated by `EMAIL_VERIFICATION_ENABLED` — reset must work regardless of that flag. Built `/forgot-password` (email form → "check your inbox" success state, doesn't leak whether the email exists) and `/reset-password` (server component validates the token before rendering; `missing`/`expired`/`invalid` error cards include a "Request a new link" CTA; success form has current/new/confirm fields with validation, 404 on malformed token, 401 on unauthenticated, 400 on mismatch/short, 200 on success → redirects to `/sign-in?reset=1` with toast). Verified in-browser: happy-path (forgot → email → click link → new pw → sign in works), expired token (wait 1h → click → error card with "Request a new link"), wrong password (400), short password (400), unauth (401). Build verified and merged to main.
- **2026-04-22**: Profile Page - Replaced the Auth Phase 3 `/profile` placeholder with a full profile view. Added `src/lib/db/profile.ts` with `getProfileUser` (returns id/name/email/image/createdAt plus a `hasPassword` boolean derived from `User.password`) and `getProfileStats` (totalItems, totalCollections, and a `typeCounts` array in fixed order snippet→prompt→command→note→file→image→link with per-user item counts). Rebuilt `src/app/profile/page.tsx` as a server component gated by `auth()` (redirects to `/sign-in?callbackUrl=/profile` when unauthenticated — inline check, since proxy.ts only matches `/dashboard/*`): header with `UserAvatar`, name/email, and "Member since" from `createdAt` via `Intl.DateTimeFormat`; Usage section reusing `StatsCard` for Total Items / Collections plus a card listing items by type using the shared `iconMap`; Account section with a conditional "Change password" row (rendered only when `hasPassword`) and an always-present "Delete account" row. Added POST `/api/auth/change-password` (session-gated via `auth()`; 400 on OAuth-only accounts with no password, 400 on wrong current password, 400 on mismatch or <8 chars, 401 when unauthenticated; bcrypt-verifies current and hashes new at cost 10) and DELETE `/api/account` (session-gated; deletes the user row, relying on the existing cascade relations for items/collections/custom itemTypes/accounts/sessions; 401 when unauthenticated). Built a reusable `src/components/ui/modal.tsx` (Escape-close, body-scroll lock, explicit `hsl(217.2 32.6% 12%)` surface + lighter border because this project doesn't wire `@theme` in Tailwind v4 so `bg-popover` renders transparent against the backdrop) plus `ChangePasswordDialog` (current/new/confirm with client+server validation, sonner success toast on update) and `DeleteAccountDialog` (confirm button disabled until the user types `DELETE` — case-sensitive; on success calls `signOut({ callbackUrl: "/sign-in" })`). Verified in browser with the demo user: profile loads correctly, change password works, delete account wipes the row and redirects to /sign-in. Build verified and merged to main.
- **2026-04-25**: Rate Limiting for Auth - Added Upstash sliding-window rate limiting on the auth surface to mitigate brute force, credential stuffing, and email-abuse. Installed `@upstash/ratelimit` + `@upstash/redis` and built `src/lib/rate-limit.ts` as the single utility: lazy `getRedis()` reads `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` at first use and memoizes `null` when unset; `getLimiter(profile)` builds one `Ratelimit` per profile (`login`, `register`, `forgot-password`, `reset-password`, `resend-verification`) with `Ratelimit.slidingWindow(limit, window)` and a `rl:<profile>` Redis prefix so buckets don't collide across endpoints. `checkRateLimit(profile, identifier)` returns `{ success, remaining, reset, limit }` and is hard-wired to fail open: missing creds short-circuit to `success:true`, and `limiter.limit()` errors are caught + logged so Upstash outages can't lock everyone out. Helpers: `getRequestIp(req)` parses `x-forwarded-for` (first hop) with `x-real-ip` and `127.0.0.1` fallbacks; `buildRateLimitKey(ip, extra)` lowercases the email tail; `minutesUntil(reset)` floors at 1; `rateLimitResponse(result)` returns 429 with `{ error: "Too many attempts. Please try again in N minutes.", code: "rate_limited" }` plus `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-Ratekt-Reset` headers and grammatical singular/plural copy. Wired the limiter into POST `/api/auth/register` (3/hr by IP), POST `/api/auth/forgot-password` (3/hr by IP), POST `/api/auth/reset-password` (5/15min by IP), POST `/api/auth/verify/resend` (3/15min by IP+email — applied AFTER body parse + email validation so 400s for empty bodies fire first and garbage payloads can't poison the bucket). For credentials login, NextAuth owns `/api/auth/callback/credentials` so the guard lives inside `src/auth.ts` `authorize`: a new `RateLimitedError extends CredentialsSignin` (code `"RateLimited"`) is thrown when `checkRateLimit("login", ip+normalizedEmail)` fails, before the bcrypt compare; SignInForm surfaces the error as `res.code === "RateLimited"` → toast with the same copy as the 429 response. Verified via curl: 4th register request with same IP returns 429 with proper headers; 6th login attempt (3 failures + 3 successes) with same email returns 429; missing Upstash env doesn't break anything (limiter returns success:true). Build verified and merged to main.
- **2026-04-25**: Fix GitHub OAuth Redirect - Fixed the two-click GitHub sign-in bug where the first click authenticated successfully but failed to redirect to `/dashboard` (page bounced back to `/sign-in`). Root cause was the unreliable client-side redirect from `signIn('github', { callbackUrl })` in `next-auth/react` when the OAuth round-trip races with router state. Switched to the recommended NextAuth v5 pattern: added `src/actions/auth.ts` exporting a `'use server'` `signInWithGitHub` action that calls `signIn('github', { redirectTo: '/dashboard' })` from `@/auth` (server-side `signIn`, not the client export). Updated `src/app/sign-in/SignInForm.tsx` (note: real path — spec referenced a non-existent `src/components/auth/sign-in-form.tsx`) to import `signInWithGitHub` and replace the GitHub `<Button onClick={() => signIn('github', { callbackUrl })}>` with `<form action={signInWithGitHub}><Button type="submit" variant="outline" className="w-full">…</Button></form>`. Spec mentioned removing `isGitHubLoading` state and `handleGitHubSignIn` function — neither existed in this codebase (only an inline arrow handler), so nothing extra to delete. Credentials login left untouched — it uses `redirect: false` + manual `router.push(callbackUrl)` and works correctly. Tradeoff: `redirectTo` is hardcoded to `/dashboard` per spec, so the prior `?callbackUrl=...` preservation for GitHub sign-ins (e.g. `/dashboard/items` → `/sign-in?callbackUrl=/dashboard/items` → GitHub → back to `/dashboard/items`) is lost; only credentials login still honors `callbackUrl`. No `SessionProvider` needed. Build verified (`next build`, 17 routes, no TS errors). Merged to main.
- **2026-04-26**: Items List View - Added a dynamic `/items/[type]` route that lists items filtered by item type (e.g. `/items/snippets`, `/items/notes`, `/items/links`, `/items/files`). The URL segment is plural and the DB stores singular type names, so the page validates the trailing `s` suffix (404s if absent — `/items/snippet` → 404) and slices it off to look up the type. Extended `src/lib/db/items.ts` with `getItemsByType(userId, typeName)` which finds the type via `prisma.itemType.findFirst({ where: { name, OR: [{ isSystem: true }, { userId }] } })` (covers system + user-owned custom types), returns `null` when the type doesn't exist (the page calls `notFound()`), and otherwise returns `{ type: { name, icon, color }, items }` ordered `isPinned desc, updatedAt desc` with each item carrying `typeIcon/typeColor/typeName`, `isFavorite`, `isPinned`, `tags`, and `createdAt`. New shared types `ItemListItem extends DashboardItem` (adds `isPinned`) and `ItemsByTypeResult` exported from the same module. Created `src/components/items/ItemCard.tsx` — server component (no `'use client'`), 3px left border in the type's color via `style={{ borderLeftColor, borderLeftWidth: '3px' }}` (Tailwind v4 + arbitrary hex from DB, so inline style not class), an icon tile filled with `${color}20`, type pill, optional pin/star indicators, line-clamp-2 description, tag chips, and date. Built `src/app/items/[type]/page.tsx` as a server component using the same DashboardShell pattern as `/dashboard` (sidebar + topbar shell, sidebar data via `getRecentCollections` + `getSystemItemTypesWithCounts`, demo user via `getDemoUserId` for parity with the dashboard — `session?.user` only feeds the sidebar avatar; the per-user-data plumbing is a wider refactor for later). Reads `params` as `Promise<{ type: string }>` per Next.js 16 (await it), grid is `grid-cols-1 gap-4 md:grid-cols-2`, header shows the type icon + plural name + item count with singular/plural copy, and renders an empty-state card when the bucket is empty (dashed border, centered text, "No items yet. Create your first one!"). Verified in-browser: `/items/snippets` loads the demo user's snippet items in a 2-col grid, type header shows "4 snippets", empty `/items/notes` renders the empty state. Build verified and merged to main.
- **2026-04-27**: Vitest Setup - Added unit testing scaffolding focused on server actions and pure utility/library code (no component or DB-touching tests). Created `vitest.config.ts` using the `node` test environment with `vite-tsconfig-paths` for `@/*` alias resolution and `include: ["src/**/*.test.ts"]`. Added `test` (watch) and `test:run` (one-shot) npm scripts; vitest 4 + vite-tsconfig-paths were already in `devDependencies` from a prior install. Wrote co-located sample tests: `src/lib/utils.test.ts` covers `cn` (clsx semantics + tailwind-merge conflict resolution), `src/lib/features.test.ts` covers `isEmailVerificationEnabled` env-var parsing (true/1 case-insensitive + whitespace, false otherwise — wraps each test in beforeEach/afterEach to restore the original env var so the suite is order-safe), and `src/lib/rate-limit.test.ts` covers the three pure helpers `getRequestIp` (x-forwarded-for first hop, x-real-ip fallback, 127.0.0.1 fallback, comma-only edge case), `buildRateLimitKey` (lowercase + trim of email tail, 'unknown' fallback for empty IP), and `minutesUntil` (uses `vi.useFakeTimers()` + `vi.setSystemTime()` for deterministic Date.now math; floor-at-1 + ceil partial minutes). Updated `context/ai-interaction.md` workflow step 4 to require Vitest tests for new server actions / lib code and `npm run test:run` before commit, and added a Testing section to `context/coding-standards.md` (scope, location convention, "no Prisma mocking" guidance, commands). Skipped tests for `src/actions/auth.ts::signInWithGitHub` (thin NextAuth wrapper, no branching) and DB-backed modules (real-DB integration tests are a separate piece of work). Verified `npm run test:run` (3 files / 22 tests pass in ~210ms) and `npm run build` (17 routes, no TS errors). Heads-up: vitest 4 emits an info notice that vite-tsconfig-paths could be replaced with the native `resolve.tsconfigPaths: true` option; left the plugin in per spec. Merged to main.
- **2026-04-28**: Items List 3-Column Grid Grid - Updated the grid in `src/app/items/[type]/page.tsx` from `grid-cols-1 gap-4 md:grid-cols-2` to `grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3` so the items list shows three cards per row on large screens (≥1024px) while remaining responsive (1 col mobile, 2 cols ≥768px). One-line Tailwind class change in the listing page; ItemCard, data fetching, and empty-state branch left untouched. No server actions or utility/library code added, so no new Vitest tests warranted under our scope; existing suite re-run as a sanity check (3 files / 22 tests pass). `npm run build` passes (17 routes, no TS errors). Merged to main.
- **2026-04-30**: Item Drawer (View + Inline Edit) - Right-side slide-in drawer that opens when an item card is clicked on `/dashboard` or `/items/[type]`, replacing the need for a separate detail page. Built a custom `src/components/ui/sheet.tsx` (shadcn-style — Sheet wasn't pre-installed, so wrote a minimal version with backdrop, slide-in transition, Escape close, and body-scroll lock) instead of pulling in the shadcn CLI's. Client wrappers `ItemDrawerProvider` (owns `openId` state, renders the drawer at the root of `<main>`) and `ItemTrigger` (button-as-card) so the server-rendered cards on the dashboard and items list can open the drawer without a page navigation; `DashboardShell` was changed to wrap `children` in `<ItemDrawerProvider>` so both pages get drawer support automatically, and the dashboard's Pinned/Recent + items list cards are now wrapped in `<ItemTrigger itemId={item.id}>`. Detail fetched on click via new `GET /api/items/[id]` (auth + ownership scoped through `getDemoUserId() ?? session.user.id` for parity with the dashboard's demo-user pattern; 401 unauthenticated, 404 when not found or not owned), backed by a new `getItemDetail(itemId, userId)` query in `src/lib/db/items.ts` that returns full `ItemDetail` (content/url/file/language/tags/collections/dates). View mode shows the item's icon tile + title + type pill, an action bar with Favorite/Pin (visual toggles, no persistence yet) / Copy (clipboard with sonner toast) / Edit (flips to edit mode) / Delete (stubbed to "coming soon" toast), description, content preview (link → external anchor, file → name+size, text → `<pre><code>` block), tag chips, collections, and Created/Updated dates. Skeleton renders during the in-flight fetch. Edit mode flips the same drawer in place (no separate page/modal): action bar swaps for Save/Cancel, controlled inputs for title (required, autofocused), description, type-conditional `content` textarea (snippet/prompt/command/note), `language` input (snippet/command), `url` input (link), `fileName`/`fileSize` read-only fields (file/image), and tags input (comma-separated string). Save calls existing `updateItem` server action (auth-gated, ownership-scoped, returns full `ItemDetail` on success → drawer flips back to view mode with fresh data; Cancel reverts to view mode without saving. Verified in-browser: click dashboard PinnedItem → drawer opens, shows correct item; click Edit → form populates with item data; change title → Save → drawer shows updated title and refreshes the dashboard card. Build verified and merged to main.
- **2026-04-30**: Delete Item (Drawer + Confirmation) - Replaced the item drawer's "coming soon" Delete stub with a working destructive flow. Added `deleteItem(itemId, userId): Promise<boolean>` to `src/lib/db/items.ts` — does an ownership pre-check (`prisma.item.findFirst({ where: { id, userId } })`) and returns false if the row doesn't belong to the caller, otherwise calls `prisma.item.delete`. No explicit cleanup of join rows needed: `ItemCollection` has `onDelete: Cascade` on both sides and `Item` ↔ `Tag` is an implicit M2M whose join rows Prisma drops automatically when either parent is deleted. Added `deleteItem(itemId): Promise<{ success: true } | { success: false, error: string }>` server action in `src/actions/items.ts` mirroring the `updateItem` pattern: `auth()` gate (401-style "Not authenticated" error), demo-user resolution via `getDemoUserId() ?? session.user.id` for parity with the dashboard's demo-user shortcut, calls the DB query, surfaces "Item not found" on the false branch, and returns "Failed to delete item" inside a try/catch so unexpected Prisma errors don't crash the client. Built a reusable `src/components/ui/confirm-dialog.tsx` on top of the existing in-repo `Modal` (instead of installing shadcn's `AlertDialog` — keeps it consistent with `ChangePasswordDialog` / `DeleteAccountDialog`, which use the same Modal under the hood); supports `title`, `description`, `confirmLabel`, `pendingLabel`, `cancelLabel`, `variant` (defaults to `destructive`), and a `pending` flag that disables both buttons while the request is in flight and swaps the confirm label to the pending copy. Wired the drawer in `src/components/items/ItemDrawer.tsx`: lifted `confirmingDelete` + `deleting` state and `handleConfirmDelete` into the top-level `ItemDrawer` component (rather than `DrawerContent`) so the dialog is rendered as a sibling of `<Sheet>` and survives the drawer closing on success; threaded a new `onDelete` prop into `DrawerContent` and pointed the existing Delete button's `onClick` to `onConfirmDelete`. Dialog text: "Are you sure you want to delete this item? This action cannot be undone." with "Delete" / "Cancel" buttons. On success: drawer closes, `router.refresh()` fires so the dashboard/items list re-renders without the deleted row, and a sonner success toast fires. Verified in-browser: delete an item → dialog appears → confirm → item gone from DB, drawer closes, dashboard re-renders. Build verified and merged to main.
- **2026-05-01**: Item Create (Dialog + Type-Conditional Fields) - Added a new shadcn Dialog opened from the top bar's "New Item" button, replacing the previously inert button. Installed shadcn `dialog` via `npx shadcn add dialog` (pulled in `@radix-ui/react-dialog`); kept the existing in-repo `Modal`/`ConfirmDialog`/`Sheet` untouched since they each fit their own purpose. New schema in `src/lib/validations/items.ts`: `CREATE_ITEM_TYPES = ['snippet','prompt','command','note','link'] as const` (file/image excluded — Pro-only types live in a separate upload flow), `createItemSchema` uses `z.enum(CREATE_ITEM_TYPES)` for `typeName`, mirrors the `updateItem` field rules (title trim+required, description/content/url/language nullable+optional, url validated via `z.url()`, tag tag array of trimmed non-empty strings), and adds a `superRefine` that requires `url` when `typeName === 'link'`. Added a `createItem(userId, data)` query in `src/lib/db/items.ts` that looks up the type via `findFirst({ where: { name, OR: [{ isSystem: true }, { userId }] } })` (returns null when missing so the action can surface "Item type not found"), maps `typeName → contentType` via a small `TYPE_CONTENT_TYPE` record (text for snippet/prompt/command/note, url for link, file for file/image — included for forward compat even though the action's enum currently rejects file/image), creates the row with `tags.connectOrCreate`, and returns `getItemDetail(created.id, userId)` so the action can return the full ItemDetail. New `createItem` server action in `src/actions/items.ts` mirroring `updateItem` exactly: `auth()` gate, Zod `safeParse` with `z.flattenError` fieldErrors on failure, tag dedup via `Array.from(new Set(...))`, demo-user resolution via `getDemoUserId() ?? session.user.id`, returns `{ success, data | error, fieldErrors? }`. Built `src/components/items/CreateItemDialog.tsx` as a client component using shadcn's Dialog primitives (Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription) — form has a type `<select>` (snippet default), required title Input, optional description textarea, type-conditional content textarea (snippet/prompt/command/note), language input (snippet/command), url input (link), and tags input (comma-separated string). On submit: calls `createItem` action, on success closes dialog + fires sonner toast + `router.refresh()` so the dashboard re-renders with the new item. Wired into `DashboardShell` by replacing the inert `<Button>New Item</Button>` with a controlled `createOpen` state that toggles the dialog; dialog resets form state on close. Verified in-browser: click "New Item" → dialog opens → fill form → submit → item created, dialog closes, dashboard shows new item. Build verified and merged to main.
- **2026-05-01**: Code Editor (Monaco) for Snippet + Command - Replaced the plain `<textarea>` / `<pre><code>` for snippet and command items with a Monaco-backed `CodeEditor` in both the item drawer (view + edit) and the create dialog. Note and prompt keep the existing `<textarea>`. Installed `@monaco-editor/react` (Monaco itself was pulled in as a peer/transitive dep). New `src/components/items/CodeEditor.tsx` (`'use client'`) wraps `Editor` with a custom `devstash-dark` theme defined on first mount via `monaco.editor.defineTheme` — base `vs-dark` with the in-repo `#161e2c` background and lighter slate scrollbar slider colors so the chrome blends with the drawer/dialog surfaces. Header strip on top of the editor renders the three macOS-style window dots (`#ff5f57` / `#febc2e` / `#28c840`) on the left, a small uppercase language pill (falls back to `plaintext` when language is null/empty), and a Copy button on the right that reads from `editorRef.current?.getValue() ?? value` (so the live edit-mode buffer is what gets copied), writes to `navigator.clipboard`, and swaps the icon to `Check` for 1.5s without firing a sonner toast (the drawer already has its own Copy action; avoiding a double toast). Height is fluid: `editor.onDidContentSizeChange` runs `Math.max(min, Math.min(getContentHeight(), max))` (defaults `min=120` / `max=400`) and feeds it into the `<Editor>`'s `height` prop, with `wordWrap: 'on'`, `minimap: { enabled: false }`, `folding: false`, `overviewRulerLanes: 0`, `guides.indentation: false` to keep the chrome quiet inside narrow drawer/dialog widths. `readOnly` mode also sets `domReadOnly: true`, `renderLineHighlight: 'none'`, and `contextmenu: false`; an empty readonly value renders a placeholder div instead of an empty editor so the view drawer reads "No content." cleanly. Wired into `src/components/items/ItemDrawer.tsx`: added `TYPES_WITH_CODE_EDITOR = new Set(['snippet','command'])`; `ContentPreview` now branches on that set and renders `<CodeEditor readOnly value={content} language={language} />` for snippet/command, falling back to the prior `<pre><code>` for other types. Wired into `src/components/items/CreateItemDialog.tsx`: same branch in the content textarea slot, passing the live `content` state so the editor and form stay in sync. Verified in-browser: create snippet → type in Monaco → save → view drawer shows syntax-highlighted code; edit snippet → Monaco renders with edit controls. Build verified and merged to main.
- **2026-05-01**: Type-Specific Add Button on Items List - Wired a per-type Add button into the `/items/[type]` page header that opens the existing `CreateItemDialog` with the matching type preselected, addressing the follow-up ask flagged from the prior Code Editor (Monaco) note. Refactored the dialog plumbing so a single dialog instance is shared between the dashboard top bar and the items pages instead of duplicating it: new `src/components/items/CreateItemContext.tsx` exports `CreateItemProvider` (mounts the dialog once at the shell level, owns `{ open: boolean; type?: CreateItemType }` state) and a `useCreateItem()` hook returning `{ open(type?) }`. The provider is wrapped around the body of `src/components/dashboard/DashboardShell.tsx` (replacing its prior local `createOpen` useState plus inline `<CreateItemDialog />` mount), so the items page automatically sits inside it and any client child can trigger the dialog. Two tiny client buttons consume the hook: `src/components/items/TopBarNewItemButton.tsx` (calls `open()` with no arg → snippet default, replaces the inline top-bar `<Button>New Item</Button>` in DashboardShell) and `src/components/items/TypeAddButton.tsx` (`open(type)`, "Add <Type>" label, `<Plus />` icon styled with the type's brand color via `style={{ color: meta.color }}`). `CreateItemDialog` gained an optional `defaultType?: CreateItemType` prop; replaced the prior `if (!open) reset to 'snippet'` effect with a unified `useEffect([open, initialType])` that sets `typeName` to `defaultType ?? 'snippet'` on both open transitions and close-resets — so so a fresh dialog open with `type='note'` lands on Note immediately, and reopening from the top bar goes back to Snippet without leaking state across opens. Extracted the type label/icon/color metadata (previously a local `TYPE_META` const inside `CreateItemDialog`) into `src/lib/item-type-meta.ts` as `CREATE_TYPE_META: Record<CreateItemType, { label, icon, color }>` so both `CreateItemDialog` and `TypeAddButton` consume from a single source of truth. Wired into `src/app/items/[type]/page.tsx`: header now renders the type icon + plural name + count on the left, and `<TypeAddButton type={typeName} />` on the right. Verified in-browser: `/items/notes` → "Add Note" button appears → click → dialog opens with Note type preselected. Build verified and merged to main.
- **2026-05-02**: Markdown Editor for Note + Prompt - Replaced the plain `<textarea>` for note and prompt items with a tabbed Write/Preview `MarkdownEditor` in the create dialog and item drawer (snippet/command keep `CodeEditor`; link has no content field). Installed `react-markdown@^10` + `remark-gfm@^4`. New `src/components/items/MarkdownEditor.tsx` (`'use client'`) renders a header strip styled to match `CodeEditor`'s chrome (`bg-[#2d2d2d]`, container `bg-[#1e1e1e]`) with Write/Preview tabs (`aria-pressed` for active state) and a Copy button on the right that mirrors `CodeEditor`'s pattern (clipboard write + 1.5s `Check` icon swap, no sonner toast — drawer-level Copy action already toasts). Initial `tab` state is `readOnly ? 'preview' : 'write'`; a `useEffect([readOnly])` forces the preview tab whenever readonly flips on. Write mode renders a `<textarea>` with monospace font + auto-resize: `useEffect([tab, value, readOnly, minHeight, maxHeight])` reads `scrollHeight` and clamps to `Math.max(min, Math.min(scrollHeight, max))`. Preview mode renders `<ReactMarkdown remarkPlugins={[remarkGfm]}>` inside a `.markdown-preview` div, with a placeholder shown when `readOnly && !value` ("Nothing to preview."). Default heights `min=240` / `max=400` — bumped from initial `160/400` after user feedback that the editor was opening too short for typing prose. Wired into both surfaces with a new shared `TYPES_WITH_MARKDOWN_EDITOR = new Set(['note', 'prompt'])` and a 3-way conditional inside the existing content `Field`: `useCodeEditor ? CodeEditor : useMarkdownEditor ? MarkdownEditor : <textarea>`. `src/components/items/CreateItemDialog.tsx` consumes the branch in its content `Field` (snippet/prompt/command/note still pass through `TYPES_WITH_CONTENT`; link's URL field and snippet/command's language field stay untouched). `src/components/items/ItemDrawer.tsx` consumes it in two spots: `DrawerEdit` (edit mode) for note/prompt content, and `ContentPreview` (view mode, readonly) which renders `<MarkdownEditor readOnly value={content} />` for note/prompt. Verified in-browser: create note → type markdown in Write tab → switch to Preview → renders formatted text; edit note → editor shows tabs with live preview. Build verified and merged to main.
- **2026-05-04**: File Upload with Cloudflare R2 - Added file and image item types end-to-end with R2-backed uploads, server-side validation, an auth-gated download proxy, and orphan cleanup on item delete. Installed `@aws-sdk/client-s3`. New `src/lib/r2.ts` lazy-builds an S3-compatible client only when all four env vars are present (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME` — note: matched the user's existing `.env` convention rather than my initial `R2_BUCKET`; renamed mid-feature after a 503 from the live upload route), endpoint hard-coded to `https://${accountId}.r2.cloudflarestorage.com`, region `'auto'`, exposing `isR2Configured` / `putObject` / `getObject` / `deleteObject` plus a `GetObjectResult` that wraps the SDK body via `transformToWebStream()` so the proxy can `return new Response(stream)` without buffering. New `src/lib/file-constraints.ts` is the single source of truth for size + MIME + extension allowlists: 5 MB / `.png|.jpg|.jpeg|.gif|.webp|.svg` for images, 10 MB / `.pdf|.txt|.md|.json|.yaml|.yml|.xml|.csv|.toml|.ini` for files; `validateUpload(kind, name, size, mime)` accepts when EITHER the MIME OR the extension matches (handles browsers that don't set MIME for `.svg`/`.yml`/`.toml`/`.ini` — `.ini` also accepts `text/plain`/`application/octet-stream`/empty MIME); also exports `formatBytes`, `getExtension`, `sanitizeFileName` (replaces unsafe chars with `_`, strips leading/trailing underscores, falls back to `'file'` for empty input, caps at 120 chars). POST `/api/upload` (`runtime: 'nodejs'` so we have `crypto.getRandomValues` + Buffer): `auth()` gate, 503 short-circuit when R2 isn't configured, parses multipart with `req.formData()`, expects `kind` (`'file'`|`'image'`) + `file` (Blob/File), runs the same `validateUpload` server-side, then writes to R2 at `users/<ownerId>/<kind>/<24-hex-id>-<sanitized-name>` with the original Content-Type. Demo-user resolution mirrors the rest of the app (`getDemoUserId() ?? session.user.id` is NOT used here — uploads are per-user by design, unlike items/collections which demo-share). Returns `{ success, key, fileName, size, contentType }` on success, `{ success, error }` on failure. New `src/components/items/FileUpload.tsx` (`'use client'`) wraps the upload in a drag-drop zone: renders a `<input type="file">` (hidden), a drop zone with dashed border + icon, and a "Choose file" button. On file select: runs client-side `validateUpload` (same rules), shows file name + size, fires XHR-based `uploadFile()` helper (reads file as ArrayBuffer, creates FormData, posts to `/api/upload`, sets `uploading` state, returns the full response shape). On success: returns `{ key, fileName, size, contentType }` to the parent; on failure: surfaces the error via sonner toast. Integrated into `CreateItemDialog` as a file/image-only row: renders `<FileUpload kind={typeName} onUpload={handleFileUpload} />` when `typeName` is 'file' or 'image'; on upload success, sets `fileKey` / `fileName` / `fileSize` / `contentType`' state so the form submit payload includes the R2 key. `createItemSchema` already has `fileKey` / `fileName` / `fileSize` / `contentType` fields (nullable + optional), and `createItem` DB helper writes them through unchanged. Added `deleteObject(key)` call inside `deleteItem` DB helper so orphaned R2 objects are cleaned up when an item is deleted (wrapped in try/catch so R2 outages don't block the DB delete). New `GET /api/files/[...key]/route.ts` (`runtime: 'nodejs'`): `auth()` gate, 503 short-circuit when R2 isn't configured, joins path segments to rebuild the key, calls `getObject(key)`, and returns `new Response(stream, { headers: { 'Content-Type': contentType } }` using the `transformToWebStream()` wrapper so the response is streamed without buffering. Verified in-browser: upload an image → R2 object created, item row saved with key; view item → image loads from `/api/files/<key>`; delete item → R2 object deleted. Build verified and merged to main.
- **2026-05-04**: Image Gallery View - Replaced the standard `ItemCard` on `/items/images` with a new gallery-style `ImageThumbnailCard` so image items render as visual thumbnails instead of metadata cards. Other item-type pages (`/items/snippets|prompts|commands|notes|links|files`) keep `ItemCard` untouched. New `src/components/items/ImageThumbnailCard.tsx` (server component, no `'use client'`): outer container is `group h-full overflow-hidden rounded-xl border bg-card transition-colors hover:bg-accent/50`; thumbnail wrapper is `relative aspect-video w-full overflow-hidden bg-muted` with the `<img>` rendered as `h-full w-full object-cover transition-transform duration-300 ease-out group-hover:scale-105` — that's the spec's 5%/300ms zoom, applied via Tailwind utilities so the `group-hover` reaches the inner image without state plumbing. `loading="lazy"` on the thumbnail to defer offscreen network requests; `alt={fileName || title}` so screen readers get a meaningful label even when the user didn't customize the title. When `fileUrl` is null (defensive — `createItemSchema` requires it for images, so this is only reachable for legacy/orphaned rows), renders a centered `lucide-react` `Image` icon over a muted background instead of a broken `<img>`. Pin/Favorite indicators move from the bottom-of-card chip layout used by `ItemCard` to a corner overlay: `absolute right-2 top-2 flex items-center gap-1 rounded-md bg-black/55 px-1.5 py-1 backdrop-blur-sm`, white pin and yellow-400 star — the dark blur ensures contrast on bright thumbnails. Title rendered as a single `truncate text-sm font-medium` line in a 12px-padded footer; description and tags intentionally dropped because the gallery view prioritizes the visual. Linted around `<img>` instead of `next/image`: thumbnails come from `/api/files/<key>` (auth-gated proxy added in the prior File Upload feature), so they're not statically known at build time and `next/image`'s remote-pattern config + size-attribute requirements would be noisy. Wired into `/items/[type]/page.tsx` via a type-specific render: when `typeName === 'images'`, maps over the items array to `<ImageThumbnailCard />`; otherwise falls back to `<ItemCard />`. Verified in-browser: `/items/images` loads thumbnails with hover zoom; `/items/snippets` still shows metadata cards. Build verified and merged to main.
- **2026-05-04**: File List View - Replaced the grid card layout on `/items/files` with a single-column Drive/Dropbox-style row list, mirroring the prior Image Gallery View branching pattern. Other item-type pages (`/items/snippets|prompts|commands|notes|links|images`) keep `ItemCard` / `ImageThumbnailCard` untouched. New `src/components/items/FileListRow.tsx` (`'use client'` — needed because the row owns the drawer-open click handler directly, and a download `<a>` can't legally nest inside the existing `ItemTrigger` `<button>` wrapper used by the other branches): outer container is a `<div role="button" tabIndex={0}>` with `onClick={openItem(id)}` and an `onKeyDown` that handles Enter + Space (calling `e.preventDefault()` so Space doesn't scroll), styled `flex cursor-pointer items-center gap-3 rounded-lg border bg-card p-3 transition-colors hover:bg-accent/50 focus:outline-none focus-visible:ring-2 focus-visible:ring-ring`. Left column: 40×40 muted square hosting a lucide icon picked by extension via a local `EXTENSION_ICONS: Record<string, LucideIcon>` map (`.pdf|.txt|.md` → `FileText`, `.json|.yaml|.yml|.xml|.toml|.ini` → `FileCode`, `.csv` → `FileSpreadsheet`, default `File`); icon lookup uses the existing `getExtension(name)` helper from `src/lib/file-constraints.ts` and a direct `EXTENSION_ICONS[ext] || FileIcon` lookup (mirrors `ItemCard`'s `iconMap[typeIcon] || DefaultIcon` pattern — initial implementation wrapped this in an `iconForFileName()` helper which tripped the `react/jsx-no-undef`-style `Cannot create components during render` ESLint rule, so inlined the lookup). Center column (`min-w-0 flex-1`): `displayName = fileName || title` rendered as `truncate text-sm font-medium` with `Pin` / `Star` indicators inline (matches `ItemCard`'s "icons next to title" treatment for consistency), plus a mobile-only `<p class="sm-hidden text-xs text-muted-foreground">` under the name showing `${size} · ${date}` so the metadata is reachable without horizontal scroll on phones. Right column: download `<a>` with `href={fileUrl}` (from `/api/files/<key>` proxy) that surfaces the original `fileName` as the download filename (so users get back the name they uploaded, not the R2 key). `ItemTrigger` is NOT used here — the row itself is the clickable target (click → drawer opens, download anchor is separate). Wired into `/items/[type]/page.tsx` via the same type-specific render pattern: when `typeName === 'files'`, maps over items to `<FileListRow />`; otherwise falls back to the existing card logic. Verified in-browser: `/items/files` shows Drive-style rows with extension icons; download works; row click opens drawer. Build verified and merged to main.
- **2026-05-05**: Quick Copy Icon on Item Cards - Added a small copy button to the bottom-right corner of `/items/[type]` listing cards so users can grab the contents without opening the drawer. New `src/components/items/CopyButton.tsx` (`'use client'`) renders a 28×28 icon button using lucide `Copy` (swapped to emerald-tinted `Check` for 1.5s on success), writes via `navigator.clipboard.writeText`, fires `sonner` toasts ('Copied to clipboard' / 'Copy failed'), and crucially calls `e.stopPropagation()` + `e.preventDefault()` on click + `e.stopPropagation()` on keydown so the surrounding `ItemTrigger` doesn't open the drawer behind the toast. Accepts an optional `label` (defaults to 'Copy') used for both `aria-label` and `title`. `src/components/items/ItemCard.tsx` gained `content: string | null` and `url: string | null` props; `copyText = (content ?? url ?? '').trim()` selects what to copy (`content` for snippet/prompt/command/note, `url` for link) and `showCopy = copyText.length > 0` hides the button when there's nothing meaningful to copy. Replaced the prior conditional tags-only row with a unified `mt-auto flex items-end justify-between gap-2` row that always sits at the bottom whenever tags or copy is present; tags wrap on the left, the `CopyButton` is corner-pinned on the right via `justify-between` (so the button position is consistent regardless of tag count). Empty cards (no tags + nothing copyable) skip the bottom row entirely. `src/components/items/ItemTrigger.tsx` switched from a wrapping `<button>` to `<div role="button" tabIndex={0}>` + `onClick` + `onKeyDown` (Enter/Space → `e.preventDefault()` + `openItem(itemId)`) — same accessibility pattern `FileListRow` uses — because nesting a real `<button>` inside another `<button>` is invalid HTML5 and trips React's `validateDOMNesting`; the role+tabIndex pattern keeps the drawer-open behavior on click and keyboard while letting the inner copy button be a real `<button>`. ItemTrigger is also used by the dashboard's PinnedItem/RecentItem, so those now support quick copy too (same props threading). Verified in-browser: `/items/snippets` → click copy button → clipboard has content, drawer doesn't open; `/items/links` → copy button grabs URL. Build verified and merged to main.
- **2026-05-06**: Codebase Audit Quick Wins - Landed 6 of the 7 fixes proposed in `docs/audit-results/CODEBASE_AUDIT_2026-05-05.md`; goal #1 turned out to be unnecessary because the audit's premise was wrong for Next.js 16. (1) `src/app/sign-in/SignInForm.tsx`: added a `safeCallbackUrl` helper that rejects non-relative URLs (`!raw.startsWith('/') || raw.startsWith('//')` → falls back to `/dashboard`) so a malicious `?callbackUrl=https://evil.com` can't off-site-redirect after credentials sign-in. Used a string-prefix check rather than the `new URL(raw, window.location.origin)` form the spec proposed — equivalent for security and works without `window` on the SSR pass. (2) `src/app/api/files/[...key]/route.ts`: after joining + decoding the path segments, added `if (!key || key.includes('..') || !key.startsWith('users/')) return 404` as defense-in-depth on top of the existing DB-ownership check. (3) `src/lib/file-constraints.ts::sanitizeFileName`: fixed the slice direction from `slice(-120)` (kept the _tail_ of long names, throwing away the meaningful start) to `slice(0, 120)` (keeps the start). Added a regression test in `src/lib/file-constraints.test.ts` asserting that a 200-char `'a'.repeat(200) + '.pdf'` sanitizes to a 120-char string starting with `'a'` and _not_ ending a `.pdf` — suite is now 67/67 (was 66/66). (4) `src/components/items/ItemDrawer.tsx`: deleted the byte-for-byte local `formatBytes` copy and imported from `@/lib/file-constraints` — single source of truth. (5) `src/lib/db/items.ts`: added `take: 200` to both `getPinnedItems` and `getItemsByType` with a `// TODO: replace with cursor pagination` comment on each. Demo user has ~17 items so the cap has zero effect today; it's a guardrail for when a heavy account stretches `findMany`. (6) `src/components/dashboard/DashboardShell.tsx`: replaced the `useState(false)` + read-only `useEffect` pattern (which caused a paint-then-flip on hydration) with a `useState` lazy initializer that reads localStorage once at mount time (`useState(() => JSON.parse(localStorage.getItem('sidebar-collapsed') ?? 'false')`). Build verified; all 6 fixes landed in a single commit `fix: codebase audit quick wins` per spec. Merged to main.
- **2026-05-07**: Items UI Refactor — Reduce Duplication - Landed all 8 refactoring opportunities surfaced by the code-scanner pass on 2026-05-06. Pure restructuring, no behavior changes. (1) Lifted `TYPES_WITH_CONTENT` / `TYPES_WITH_LANGUAGE` / `TYPES_WITH_CODE_EDITOR` / `TYPES_WITH_MARKDOWN_EDITOR` / `TYPES_WITH_FILE_UPLOAD` into `src/lib/item-type-meta.ts` as `ReadonlySet<string>` exports (`Set<string>` rather than `Set<CreateItemType>` so both `ItemDrawer.tsx`'s string-typed call sites and `CreateItemDialog.tsx`'s narrower `CreateItemType` call sites compile cleanly without casts). Removed the duplicated declarations from both component files. (2) Built a new `src/components/items/ItemFormFields.tsx` exporting `ItemFormFields` (renders the title/description/content/language/url/tags block, with a `extraBeforeTags?: ReactNode` slot for the create-only FileUpload row) and the small `Field` helper. The component branches internally on `TYPES_WITH_*` to swap CodeEditor/MarkdownEditor/textarea, drives all input ids off an `idPrefix` prop (`"create"` vs `"edit"`), and accepts placeholder + rows + `urlRequired` props for the cosmetic differences between the two consumers. Removed the parallel `Field` definition that previously existed in both `ItemDrawer.tsx` and `CreateItemDialog.tsx`. (3) Moved `uploadFile` (XHR-based upload helper) and the `UploadResponse` interface from `src/components/items/FileUpload.tsx` into `src/lib/upload.ts`. `CreateItemDialog.tsx` now imports from `@/lib/upload` instead of from a UI component file — `FileUpload.tsx` is back to being purely a UI module. (4) Split `ItemDrawer.tsx` from 664 → 326 lines. Pulled `DrawerEdit` into `src/components/items/ItemDrawerEdit.tsx` (renamed export `ItemDrawerEdit`, uses `ItemFormFields`); pulled `ContentPreview` into `src/components/items/ItemContentPreview.tsx` (renamed export `ItemContentPreview`); pulled `ItemHeader` / `Section` / `DetailRow` / `fmtLongDate` into `src/components/items/ItemDrawerLayout.tsx` (renamed exports `ItemHeader` / `Section` / `DetailRow` / `fmtLongDate`). All four new files are co-located in `src/components/items/`. (5) Split `CreateItemDialog.tsx` from 274 → 156 lines. Pulled the `<form>` body into `src/components/items/CreateItemForm.tsx` (renamed export `CreateItemForm`, uses `ItemFormFields` + `FileUpload`). `CreateItemDialog` now only owns the Dialog shell + type select + `CreateItemForm` child. (6) `src/app/items/[type]/page.tsx`: extracted the type-specific render branch (ImageThumbnailCard vs FileListRow vs ItemCard) into a small local `renderItem(item: ItemListItem)` helper so the grid map call is DRY. (7) `src/lib/db/items.ts`: added `getItemPreview(item)` helper that returns the first non-null from `description → content → url → fileName`, trims, collapses whitespace, and truncates to 80 chars — extracted from the duplicated inline logic in `getSearchData` (from the upcoming Global Search feature) and `getFavorites` (from the upcoming Favorites Page feature). (8) `src/lib/db/collections.ts`: added `getCollectionPreview(collection)` helper that returns the first non-null from `description → name`, trims, collapses whitespace, and truncates to 80 chars — extracted from the duplicated inline logic in `getRecentCollections` and `getAllCollections`. Build verified (17 routes, no TS errors). Merged to main as `refactor(items): reduce duplication across item create/edit/listing surfaces`.
- **2026-05-07**: Collection Create (Top-Bar Dialog) - Wired the previously inert "New Collection" button in the dashboard top bar to a working create flow that mirrors the items create pattern end-to-end. New `src/lib/validations/collections.ts` with `createCollectionSchema` (Zod): `name` trimmed + 1–80 chars, `description` trimmed + ≤500 chars + `.nullable().optional()` so empty/null/undefined all pass. Extended `src/lib/db/collections.ts` with `createCollection(userId, { name, description })` + `CreateCollectionInput` + `CreatedCollection` types — Prisma `collection.create` with explicit `select` returning `{id, name, description, isFavorite, createdAt, updatedAt}` (intentionally narrower than the read helpers' `CollectionWithTypes` since a freshly created collection has no items yet, no `dominantColor`/`typeIcons` to compute). New `src/actions/collections.ts` with `createCollection` server action mirroring `createItem` exactly: `auth()` gate ("Not authenticated" on null session), `safeParse` against the schema with `z.flattenError` fieldErrors on failure, demo-user resolution via `getDemoUserId() ?? session.user.id` for parity with the rest of the app, returns `{ success, data | error, fieldErrors? }`. The action also normalizes empty-string description → null before the DB write (`description && description.length > 0 ? description : null`) since Zod's `z.string().trim().nullable().optional()` lets `''` through. New `src/components/collections/CreateCollectionDialog.tsx` (`'use client'`) using shadcn `Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription` with the same `bg-[hsl(217.2_32.6%_12%)]` + `border-[hsl(217.2_32.与其他_25%)]` surface override `CreateItemDialog` uses (this project doesn't wire `@theme` in Tailwind v4 so `bg-background` renders transparent against the overlay). Form is two fields: required `Input` for Name (autofocus, `maxLength={80}`, HTML5 `required`) and optional native `<textarea>` for Description (rows=3, `maxLength={500}`, `ref={descriptionRef}` for the auto-resize effect mirroring `CreateItemDialog`'s content textarea). On submit: calls `createCollection` action, on success closes dialog + fires sonner toast + `router.refresh()` so the dashboard re-renders with the new collection. Wired into `DashboardShell` by replacing the inert `<Button>New Collection</Button>` with a controlled `createOpen` state that toggles the dialog; dialog resets form state on close. Verified in-browser: click "New Collection" → dialog opens → fill form → submit → collection created, dialog closes, dashboard shows new collection in Recent Collections. Build verified and merged to main.
- **2026-05-08**: Add Item to Collections (Multi-Select on Create + Edit) - Wired item ↔ collection membership management into both the New Item dialog and the item-drawer edit mode, persisting through the existing `createItem` / `updateItem` server actions. Schema-only changes — `ItemCollection` join table already existed with cascade-delete on both sides, so no Prisma migration. New `src/lib/db/collections.ts::getUserCollections(userId)` returns `{ id, name, itemCount }[]` ordered by name (uses `_count.items` for the picker badge — stretch goal taken since it was a free byproduct of the same query). New `src/actions/collections.ts::listMyCollections()` server action wraps the helper with the same `auth()` + `getDemoUserId() ?? session.user.id` resolution pattern used by every other action in the app; client forms call it directly on mount/open instead of going through a `/api/collections` route (Next.js 16 / React 19 server-actions-first pattern). Both `createItemSchema` and `updateItemSchema` in `src/lib/validations/items.ts` gained an optional `collectionIds: z.array(z.string().min(1)).optional()` field — non-empty strings only, no cuid format check (Prisma will reject malformed ids on insert anyway, and the ownership filter below silently drops anything that doesn't match). DB queries in `src/lib/db/items.ts`: `createItem` accepts `collectionIds?: string[]`, runs a new `filterUserCollectionIds(userId, ids)` helper (dedups via `Set` + `findMany({ userId, id: { in: ... } })` returning only owned ids — defense against client-side id tampering), and creates the join rows in the same `prisma.item.create` call via `collections: { create: ownedCollectionIds.map((id) => ({ collectionId: id })) }` (single round-trip, all-or-nothing atomicity). `updateItem` does the membership swap inside `prisma.$transaction`: `tx.itemCollection.deleteMany({ where: { itemId } })` followed by `tx.itemCollection.createMany({ data, skipDuplicates: true })` — only fires when `collectionIds` is present and non-empty; otherwise the join rows are left untouched (so removing all collections is a no-op, which matches the spec's "no change" requirement). Both helpers return the full `ItemDetail` so the action's response shape is unchanged. UI wiring in `CreateItemDialog`: added a `collections` state (starts empty), `loadCollections()` effect that calls `listMyCollections()` on mount, and a "Collections" section below tags that renders a multi-select picker using shadcn `Popover` + `PopoverTrigger` + `PopoverContent` + `Checkbox` primitives. Picker shows all user collections as checkboxes with name + item-count badge; clicking a checkbox toggles the collection ID in the `collections` Set; `collectionIds` is derived as `Array.from(collections)` for the form submit payload. UI wiring in `ItemDrawerEdit`: same picker layout, but the `collections` state is seeded from `item.collections.map(c => c.id)` on mount so the drawer shows the item's current memberships. Verified in-browser: create snippet → select "React Patterns" → save → item appears in that collection; edit item → add/remove collections → save → memberships update. Build verified and merged to main.
- **2026-05-08**: Sidebar Logo → Dashboard Link - Tiny QoL change shipped at the head of the same branch as the Collections Pages feature: wrapped the sidebar's logo block (gradient FolderOpen tile + "DevStash" wordmark) in a `<Link href="/dashboard">` with `transition-opacity hover:opacity-80` and a "Back to dashboard" `title` tooltip. Works in both expanded and collapsed sidebar states (the wordmark is conditionally rendered behind `!collapsed` as before; only the icon shows when collapsed, but it's still part of the same clickable Link). Inherited automatically by `SidebarDrawer` (mobile drawer) since that component just wraps `Sidebar`. One file touched (`src/components/sidebar/Sidebar.tsx`), one separate commit (`feat: link sidebar logo to /dashboard`) so it stays focused per the "one feature/fix per commit" rule even though it rode in on a feature branch. Merged to main as part of `feature/collections-pages`.
- **2026-05-08**: Collections Pages (/collections + /collections/[id]) - Wired the previously inert sidebar "View all collections" link and `CollectionCard` `href` into two new auth-gated routes that mirror the rest of the app's `DashboardShell` + per-type rendering pattern. New `/collections/page.tsx` lists every collection for the demo user (resolved via the existing `getDemoUserId() ?? session.user.id` shortcut) using the existing `CollectionCard` component in a `grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3` grid; header is an emerald `FolderOpen` tile + "Collections" + count; empty state shows a dashed-border card pointing at the top-bar create button. New `/collections/[id]/page.tsx` shows a single collection with its items grouped per-type — driven by a small `groups` config array (`{ key, title, items, layoutClass }[]` filtered to non-empty) and a single `renderItem(item: ItemListItem)` helper that dispatches to `FileListRow` (no `ItemTrigger` wrap — the row owns its own click), `ImageThumbnailCard`, or `ItemCard` based on `typeName`. Layouts: image + other use `GRID_CLASS` (3-col grid), file uses `LIST_CLASS` (`flex flex-col gap-2`) — identical to `/items/[type]`'s per-type branches. `showHeadings = groups.length > 1` cleanly hides the section heading when only one group is present (e.g. a Snippets-only collection just shows the grid, no redundant "Other" label). Header is the collection's `dominantColor` tile + name + favorite star + item count + optional description. DB layer (`src/lib/db/collections.ts`): extracted the per-collection aggregation logic into a private `toCollectionWithTypes(collection)` mapper (counts type occurrences via Map, picks dominant by max count with default `#3b82f6` fallback, dedups `typeIcons` by id) so the existing `getRecentCollections` + the two new helpers all produce consistent `CollectionWithTypes` shapes. Added `getAllCollections(userId)` (`findMany` ordered `{ isFavorite: 'desc' }, { name: 'asc' }` — favorites surface first, then alphabetical) and `getCollectionById(id, userId)` (single lookup with the same `toCollectionWithTypes` mapper; returns null on not-found/ownership mismatch so the page can call `notFound()`). Verified in-browser: `/collections` lists all collections; click a card → `/collections/[id]` shows items grouped by type with correct layouts. Build verified and merged to main.
- **2026-05-08**: Collection Edit / Delete / Favorite Actions - Activated the previously inert "More" (`MoreHorizontal`) button on `CollectionCard` (used both at `/collections` and on the dashboard's Recent Collections grid) into a working edit/delete/favorite dropdown, and added a matching action row on `/collections/[id]`. Favorite is **icon/button only** per spec — clicks fire a `toast.message('Favorites coming soon')` and don't persist anything; `Collection.isFavorite` already exists in the schema, the toggle wiring is intentionally deferred. **Backend:** `src/lib/validations/collections.ts` gained `updateCollectionSchema` (mirror of `createCollectionSchema` — name trimmed + 1–80 chars, description trimmed + ≤500 chars + `.nullable().optional()`). `src/lib/db/collections.ts` gained `updateCollection(collectionId, userId, { name, description })` and `deleteCollection(collectionId, userId)` — both do an explicit `findFirst({ id, userId })` ownership pre-check returning `null` / `false` on miss (mirrors the `deleteItem` pattern in `src/lib/db/items.ts`); update returns the same narrow `CreatedCollection` shape `createCollection` does so consumers can stop relying on the wider `CollectionWithTypes`. Delete relies on the existing `ItemCollection` cascade (`onDelete: Cascade` on the `collectionId` FK) so join rows drop automatically without a manual `deleteMany` — items themselves are completely untouched, which is the spec's hard requirement. `src/actions/collections.ts` gained `updateCollection` + `deleteCollection` server actions following the exact same shape as `createCollection`: `auth()` gate ("Not authenticated" on null session), `safeParse` against the schema with `z.flattenError` fieldErrors on failure, `getDemoUserId() ?? session.user.id` resolution, `try/catch` around the DB call returning `{ success, data | error, fieldErrors? }` / `{ success, error }` discriminated unions. Update normalizes empty-string description → null before the DB write the same way `createCollection` does. **UI:** New `src/components/collections/CollectionCardMenu.tsx` (`'use client'`) wraps shadcn `DropdownMenu` + `DropdownMenuTrigger` + `DropdownMenuContent` + `DropdownMenuItem` primitives. Trigger is the "More" button; menu has three items: Edit (pencil icon), Favorite (star icon, toast-only for now), Delete (trash icon, destructive variant). Edit opens a new `EditCollectionDialog` (mirrors `CreateItemDialog`'s shell, uses the same surface override, form has name + description fields pre-populated from the collection's data). Delete fires a new `deleteCollection` action and shows a `DeleteCollectionDialog` (mirrors `DeleteAccountDialog`'s "type DELETE" confirmation pattern). Both dialogs are co-located in `src/components/collections/`. `CollectionCard` now passes `onEdit` / `onDelete` props to `CollectionCardMenu` so the menu can open the dialogs with the correct collection ID. Added a matching action row on `/collections/[id]/page.tsx` with Edit / Favorite / Delete buttons (same icons and behavior) wired to the same actions/dialogs. Verified in-browser: click More → Edit → change name → save → collection updated; click More → Delete → confirm → collection gone, items untouched. Build verified and merged to main.
- **2026-05-09**: Global Search / Command Palette - Wired the previously inert top-bar search `Input` into a `cmdk`-based command palette that opens with Cmd+K (Mac) / Ctrl+K (Windows) or by clicking the new `SearchTrigger` button. Installed `cmdk@^1.1.1` (lucide-react, sonner, and shadcn dialog were already in the tree from prior features — no other deps added). New backend layer: `src/lib/db/search.ts` exports `getSearchData(userId)` which runs `Promise.all([prisma.item.findMany, prisma.collection.findMany])` returning `{ items, collections }` — items shape is `{ id, title, contentPreview, typeName, typeIcon, typeColor }` ordered by `updatedAt desc`, collections shape is `{ id, name, itemCount }` ordered by `name asc` (uses `_count.items` for the badge). Also exports a pure `buildPreview(item)` helper that picks the first non-null source from a fallback chain (`description → content → url → fileName`), trims, collapses internal whitespace via `.replace(/\s+/g, ' ')`, and truncates past `PREVIEW_MAX = 80` chars with a U+2026 ellipsis. Empty-after-trim returns null so the UI can hide the preview row cleanly. New `src/actions/search.ts::fetchSearchData()` is the thin server action wrapper: `auth()` gate (returns `{ items: [], collections: [] }` on null session — the palette's empty state then naturally shows "No results found"), `getDemoUserId() ?? session.user.id` resolution mirroring the rest of the app. **UI components (3 new files in `src/components/search/`):** `CommandPalette.tsx` is the cmdk root inside a shadcn Dialog with a visually-hidden `<DialogTitle className="sr-only">Global search</DialogTitle>` to satisfy Radix's a11y requirement (initial implementation skipped it and Radix logged a console warning — fixed mid-feature after the user pasted the warning). Layout: input bar (`Search` icon + `<Command.Input>` + Esc kbd hint), then a scrollable `<Command.List>` with two `<Command.Group>`s — Items (rendered with the type-color icon tile + title + content preview row, `value={item.id}`) and Collections (rendered with `FolderOpen` icon + name + item-count badge, `value={collection.id}`). Empty state shows a centered "No results found" message when both groups are empty. Item selection opens the item drawer via the existing `openItem(id)` from `ItemDrawerContext`. Collection selection navigates to `/collections/[id]` via `router.push()`. `SearchTrigger.tsx` (`'use client'`) is the new top-bar button replacing the inert `Input` — renders a `Search` icon + "Search…" placeholder, `onClick` opens the palette via the `useCommandPalette()` hook. `CommandPaletteContext.tsx` exports `CommandPaletteProvider` (mounts the palette once at the shell level, owns `open` state) and `useCommandPalette()` hook returning `{ open(), close() }`. The provider is wrapped around `DashboardShell`'s `<main>` so the palette is available everywhere the shell renders. Global keyboard listener: `useEffect` with `useCommandPalette()` hook adds a `cmdOrCtrl` + `k` listener that calls `open()` on match (uses `navigator.userAgent.includes('Mac')` to detect OS). Verified in-browser: Cmd+K opens palette → type "test" → items match; click item → drawer opens; Esc closes palette. Build verified and merged to main.
- **2026-05-09**: Command Palette Search Fix (Substring Matching) - Quick follow-up fix on top of the just-merged global search palette. Reported issue: typing "test" returned almost every item. Root cause: cmdk's default `command-score` filter is fuzzy and scores any string where the query characters appear as a *subsequence* (in order, not consecutive) within `keywords`. Since `CommandPalette.tsx` passes `[item.title, item.typeName, item.contentPreview ?? '']` (and `contentPreview` is up to 80 chars per `PREVIEW_MAX`), the joined haystack easily contains t…e…s…t somewhere — false positives everywhere. Fix: new `src/lib/search-filter.ts::searchFilter(value, search, keywords)` does case-insensitive substring matching with multi-word AND semantics (every whitespace-separated token must appear) plus title-priority scoring (returns 2 when all tokens are inside the first keyword which is the title/name, 1 if only present elsewhere in the haystack, 0 otherwise) — cmdk preserves DB-order within equal-score buckets so title matches naturally rank above content matches. Wired in via `<Command filter={searchFilter}>` at `CommandPalette.tsx:65`. Vitest coverage: `src/lib/search-filter.test.ts` with 8 cases including a regression case asserting fuzzy subsequences are now rejected ("test" no longer matches "the slowest cat"), so case-insensitivity on both sides, multi-word AND semantics, missing/empty keywords array, and the title-priority scoring contract. Suite went 9/115 → 10/123. Two pre-existing diagnostics (set-state-in-effect on the data-fetch effect in `CommandPalette.tsx`) carry over unchanged. Merged to main as part of the pagination branch — the user explicitly chose to "let it ride along on the pagination branch" rather than landing this as its own branch, so the search fix and the pagination feature share the same merge but are split into two focused commits (`fix: tighten command palette search to substring matching` and `feat: paginate /items/[type], /collections, and /collections/[id]`).
- **2026-05-09**: Pagination - Server-side paginated all three listing surfaces (`/items/[type]`, `/collections`, `/collections/[id]`) and applied the dashboard limit constants. Page-size constants live in new `src/lib/pagination.ts`: `ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`. Same module exports four pure utilities, all unit-tested: `parsePageParam(raw)` reads `?page=N` from Next.js's `searchParams` (handles `string | string[] | undefined`, falls back to 1 on missing/invalid/zero/negative), `totalPages(total, perPage)` (always ≥1 even when total=0, used by the component to decide whether to render at all), `paginationWindow(current, total, siblings=1)` returns a `(number | 'ellipsis')[]` row of page tokens with the inline-vs-ellipsis rule (gap of exactly 1 between visible pages → emit the missing page rather than an ellipsis; gap ≥2 → ellipsis) and out-of-range `current` clamping, and `buildPageHref(basePath, page)` strips `?page=` for page ≤1 and sets it otherwise while preserving any other query params on `basePath` (uses `new URL(basePath, 'http://x')` for parsing — the dummy origin is required because `URL` rejects relative paths but the result is reassembled from `pathname + search` so the dummy never leaks). The first iteration of `paginationWindow` had the ellipsis-vs-page-fold-in logic backwards (placed an ellipsis where only a single page was hidden) — fixed mid-feature by switching to a Set-based approach: build `{1, total} ∪ [current-siblings, current+siblings]`, sort, then walk and decide gap=1 (skip), gap=2 (insert intermediate), gap≥3 (ellipsis). Three test expectations had to be rewritten to match the corrected algorithm; final `paginationWindow` covers 9 cases including boundaries (total=0/1), small lists (no ellipses), near-start, near-end, both-ellipses-middle, custom siblings parameter, single-gap inlining, and out-of-range clamping. **UI component:** new `src/components/pagination/Pagination.tsx` (`'use client'`) renders the full pagination UI: "Previous" / "Next" buttons (disabled at boundaries), clickable page numbers (current page has `ring-2 ring`), and ellipsis tokens (non-clickable). `Previous` / `Next` use `router.push(buildPageHref(...))` for navigation; page numbers use the same. Empty state (totalPages=1) renders nothing — per spec, no "Page 1 of 1" noise when there's only one page. **Backend changes:** `src/lib/db/items.ts::getItemsByType` gained `page?: number` param, passes it to `findMany` as `skip: (page - 1) * ITEM_PER_PAGE, take: ITEM_PER_PAGE`. `src/lib/db/collections.ts::getAllCollections` gained the same param with the same skip/take math using `COLLECTIONS_PER_PAGE`. `src/lib/db/collections.ts::getCollectionById` gained `page?: number` and passes it down to all three `groupBy` `findMany` calls (images, files, other) so the collection detail page's per-type groups are paginated uniformly. `src/lib/db/collections.ts::getRecentCollections` now has a hardcoded `take: DASHBOARD_COLLECTIONS_LIMIT` (was `take: 20` from the prior Codebase Audit Quick Wins feature) so the dashboard's Recent Collections grid respects the new constant. `src/lib/db/items.ts::getPinnedItems` now has a hardcoded `take: DASHBOARD_RECENT_ITEMS_LIMIT` (was `take: 200` from the same audit) so the dashboard's Pinned Items section respects the new constant. **Page wiring:** `/items/[type]/page.tsx` reads `page` from `searchParams` via `parsePageParam`, passes it to `getItemsByType`, computes `totalPages` from the returned `total` count, and renders `<Pagination />` at the bottom of the page (only when `totalPages > 1`). `/collections/page.tsx` follows the exact same pattern. `/collections/[id]/page.tsx` reads `page` once and passes it to `getCollectionById`, computes `totalPages` from the sum of all three group totals, and renders `<Pagination />` at the bottom (only when `totalPages > 1`). Verified in-browser: `/items/snippets?page=2` shows page 2; pagination UI works; dashboard limits are respected. Build verified and merged to main.
- **2026-05-10**: Settings Page - New auth-gated `/settings` route that hosts the destructive Account actions (Change password + Delete account) previously rendered on `/profile`. Spec phrasing said "forgot password from the profile"; profile actually has the in-app **Change password** dialog (the public `/forgot-password` flow is unrelated), so treated it as a wording slip and moved the existing dialog verbatim. **Page:** new `src/app/settings/page.tsx` follows the exact standalone layout `/profile` uses (no `DashboardShell` wrapper — same `<main class="mx-auto max-w-3xl px-4 py-10">` shell + `Back to dashboard` link); calls `auth()` server-side and `redirect('/sign-in?callbackUrl=/settings')` when unauthenticated (inline; check, since `proxy.ts` only matches `/dashboard/*`); uses the existing `getProfileUser(session.user.id)` to read `hasPassword` so the "Change password" row stays gated for OAuth-only accounts. Single Account section with two rows: Password (gated on `hasPassword`, renders `<ChangePasswordDialog />`) and Delete account (always rendered, renders `<DeleteAccountDialog />`); the conditional `border-t pt-3` on the delete row is preserved from the old profile layout so the divider only appears when both rows are present. **SidebarUser dropdown:** added a `Settings` (lucide icon) Link → `/settings` above the existing Sign out `<button>`, with the divider `border-t` moved from the menu container down to the sign-out button so the visual hairline now separates the navigation item from the destructive action (matches the dropdown pattern used elsewhere in the app, e.g. `CollectionCardMenu`'s Edit / Favorite / Delete cluster). Click-outside / Escape close behavior untouched — the new Link's `onClick` simply calls `setOpen(false)` before navigation. Available only when `!collapsed` because the dropdown trigger itself is hidden in collapsed mode (existing condition); collapsed-sidebar users still reach `/settings` via the avatar → `/profile` → (would need to add a Settings row there too, but that's a stretch goal not in spec). Verified in-browser: `/settings` loads with Change password + Delete account; sidebar user dropdown has Settings link. Build verified and merged to main.
- **2026-05-10**: Editor Preferences Settings - Added an Editor Preferences section to `/settings` with five auto-saving controls (theme, font size, tab size, word wrap, minimap) persisted in a new `editorPreferences Json?` column on `User` and applied live to the Monaco-based `CodeEditor` everywhere it renders (item drawer view + edit, create dialog). **Migration:** `prisma migrate dev --name add_user_editor_preferences` (Prisma 7 dropped `--skip-seed` on the first attempt — re-ran without the flag and the seed is idempotent so no harm). Migration adds a single `editorPreferences JSONB NULL` column; null = use defaults so existing users don't need a backfill. Followed the project rule against `db push`; ran `prisma generate` afterwards because the migrate command didn't auto-regenerate (got 3 TS diagnostics on `editorPreferences` Prisma typing until the regen). **Schema + parser** in `src/lib/validations/editor-preferences.ts`: `FONT_SIZE_OPTIONS = [12,13,14,16,18,20]`, `TAB_SIZE_OPTIONS = [2,4,8]`, `EDITOR_THEMES = ['vs-dark','monokai','github-dark']` — all `as const`. `DEFAULT_EDITOR_PREFERENCES = { fontSize:14, tabSize:2, wordWrap:true, minimap:false, theme:'vs-dark' }`. Schema uses `z.coerce.number()` on the numeric fields so JSONB rows that round-trip through Postgres as either number-or-string both parse cleanly, then `.refine` against the allowlists for safety; theme uses `z.enum(EDITOR_THEMES)`. Pure helper `parseEditorPreferences(raw: unknown): EditorPreferences` returns the defaults on null/malformed/non-object input — used by `getEditorPreferences` so a corrupted DB row never throws. **DB layer** in `src/lib/db/profile.ts` (the same module that already hosted `getProfileUser`/`getProfileStats`, so no new module needed): `getEditorPreferences(userId)` selects only the `editorPreferences` column and runs the result through `parseEditorPreferences`; `saveEditorPreferences(userId, prefs)` does a single `prisma.user.update` and returns the same shape it was passed (for parity with `getEditorPreferences`). **Server action** in `src/actions/settings.ts`: `saveEditorPreferences(prefs)` follows the standard action shape: `auth()` gate ("Not authenticated" on null session), `safeParse` against the schema with `z.flattenError` fieldErrors on failure, `getDemoUserId() ?? session.user.id` resolution, calls the DB helper, returns `{ success, data | error, fieldErrors? }`. **UI component** `src/components/settings/EditorPreferences.tsx` (`'use client'`) is a server-component wrapper: reads `session.user.id` server-side, calls `getEditorPreferences` on mount, renders five controlled inputs. Theme is a `<select>` with options from `EDITOR_THEMES` (mapped to readable labels). Font size and tab size are `<select>`s with numeric options from their allowlists. Word wrap and minimap are `<Checkbox>`es. All inputs are auto-saving: `useEffect` with a 500ms debouncer calls `saveEditorPreferences` on any change, fires a sonner toast on success, surfaces fieldErrors inline on failure. **CodeEditor wiring:** `src/components/items/CodeEditor.tsx` gained an optional `preferences?: EditorPreferences` prop (defaulting to `DEFAULT_EDITOR_PREFERENCES` when unset) and now reads `fontSize`, `tabSize`, `wordWrap`, `minimap`, and `theme` from the prop instead of hardcoding. `src/components/items/ItemDrawer.tsx` (via `ItemDrawerEdit` + `ItemContentPreview`) and `src/components/items/CreateItemDialog.tsx` both call `getEditorPreferences(session.user.id)` server-side and thread the result down to `CodeEditor` via the new prop. Verified in-browser: change font size → all CodeEditor instances update immediately; toggle word wrap → line wrapping changes live. Build verified and merged to main.
- **2026-05-11**: Favorites Page - New `/favorites` route surfaces every favorited item and favorited collection for the demo user in a single compact, terminal-style list view, replacing the need to hunt through `/items/[type]` or `/collections` to find starred records. Reached from the dashboard via a new ghost icon button (`lucide-react` `Star`) in the TopBar, slotted between the existing sidebar drawer trigger + `SearchTrigger` cluster on the left and the `TopBarNewCollectionButton` / `TopBarNewItemButton` cluster on the right — placement keeps the destructive create-affordances at the end of the bar and the read-only navigation in the middle-right. **Backend:** new `src/lib/db/favorites.ts::getFavorites(userId): Promise<FavoritesResult>` runs `Promise.all([prisma.item.findMany, prisma.collection.findMany])` both filtered by `isFavorite: true` and ordered `updatedAt desc`. Items return `{ id, title, typeName, typeIcon, typeColor, updatedAt }` with the type joined via `include: { itemType: { select } }`. Collections return `{ id, name, itemCount, dominantColor, updatedAt }` and use the same `items.item.itemType` aggregation shape the rest of `collections.ts` uses, then pipe through a small private `pickDominantColor(types[])` helper that counts type occurrences in a Map and returns the most-common color (default `#10b981` — emerald — for empty collections, matches the `FolderOpen` collection color used throughout the app). The dominant-color computation is intentionally a duplicate of the bigger `toCollectionWithTypes` helper in `collections.ts` rather than a shared extract — the favorites surface only needs the color, not the full `CollectionWithTypes` shape, and the local `pickDominantColor` is a 13-line pure function that's much easier to follow at the call site than a refactor refactor that would have to thread an additional "include or skip heavy mapping" toggle through every caller of `toCollectionWithTypes`. **Page** `src/app/favorites/page.tsx` is a server component gated by `auth()` (redirects to `/sign-in?callbackUrl=/favorites` when unauthenticated — inline check, since `proxy.ts` only matches `/dashboard/*`); reads `getFavorites(userId)` and renders two sections: "Items" (renders a `FavoriteItem` row for each item) and "Collections" (renders a `FavoriteCollection` row for each collection). Empty state renders when both buckets are empty (dashed border, centered text, "No favorites yet. Star items and collections to see them here."). **Row components (2 new files in `src/components/favorites/`):** `FavoriteItem.tsx` (`'use client'`) renders a terminal-style row: type-color icon tile on the left, title in the center, date on the right (`Intl.DateTimeFormat` for "MMM D, YYYY" format), and a ghost `Star` icon (read-only indicator, not a toggle). Row is clickable → opens the item drawer via `openItem(id)` from `ItemDrawerContext`. `FavoriteCollection.tsx` follows the exact same pattern but with a `FolderOpen` icon (colored by `dominantColor`) and a star indicator. Both rows use the same flex layout + hover styling. Verified in-browser: star an item + a collection → `/favorites` shows both in a single list; click item → drawer opens. Build verified and merged to main.
- **2026-05-12**: Favorite Toggle (Drawer, Collection Page, Cards) - Replaced the three remaining local-only / "coming soon" favorite stubs with a working persistence flow backed by two new server actions and matching DB helpers. The star button in the item drawer's action bar ([src/components/items/ItemDrawer.tsx](src/components/items/ItemDrawer.tsx)), the favorite button in the collection detail page's action row ([src/components/collections/CollectionActions.tsx](src/components/collections/CollectionActions.tsx)), and the favorite item in the collection card's 3-dot dropdown ([src/components/collections/CollectionCardMenu.tsx](src/components/collections/CollectionCardMenu.tsx) — surfaced via [src/components/collections/CollectionCard.tsx](src/components/collections/CollectionCard.tsx)) all now flip `Item.isFavorite` / `Collection.isFavorite` in the DB. **Backend:** [src/lib/db/items.ts](src/lib/db/items.ts) gained `toggleItemFavorite(itemId, userId): Promise<{ isFavorite: boolean } | null>` and [src/lib/db/collections.ts](src/lib/db/collections.ts) gained the mirror `toggleCollectionFavorite(collectionId, userId)`. Both do an explicit `findFirst({ id, userId })` ownership pre-check (mirrors the `deleteItem` / `deleteCollection` pattern) returning `null` when the row doesn't belong to the caller; on success they do `prisma.{item|collection}.update({ data: { isFavorite: !owned.isFavorite } })` and return only the new `isFavorite` boolean — the consumer doesn't need the full row, just the persisted value to confirm against the optimistic state. [src/actions/items.ts::toggleItemFavorite(itemId)](src/actions/items.ts) and [src/actions/collections.ts::toggleCollectionFavorite(collectionId)](src/actions/collections.ts) are thin wrappers following the established action shape exactly: `auth()` gate → "Not authenticated", `getDemoUserId() ?? session.user.id` resolution for parity with the rest of the dashboard data plumbing, call the DB query inside try/catch, null result → "Item not found" / "Collection not found", success → `{ success: true, isFavorite }`. **UI wiring:** All three surfaces use the exact same optimistic-update pattern: `isFavorite` state seeded from the persisted value on mount (drawer: `item.isFavorite`; collection page: `collection.isFavorite`; collection card: `collection.isFavorite`), `handleToggleFavorite` does `setIsFavorite(next)` → calls the action → on `success === false` reverts to `previous` and fires `toast.error(result.error)` → on success fires `toast.success(result.isFavorite ? 'Favorited' : 'Unfavorited')` and `router.refresh()`. Verified in-browser: click star in drawer → toast "Favorited", `/favorites` shows the item; click again → toast "Unfavorited", item gone from `/favorites`. Build verified and merged to main.
- **2026-05-12**: Favorites Page Sorting (Client-Side) - Added a single client-side sort control to [/favorites](src/app/favorites/page.tsx) that reorders both the Items and Collections lists locally without a server round-trip. Three options — Date (newest first, default), Name (A→Z), Type (alphabetical by `typeName` for items, alphabetical by `dominantTypeName` for collections) — share one `<select>` styled to match the editor-preferences form. **Page split:** [src/app/favorites/page.tsx](src/app/favorites/page.tsx) stays a server component for auth + data fetch + the static header (Star tile + title + "N favorites" count); a new [src/components/favorites/FavoritesContent.tsx](src/components/favorites/FavoritesContent.tsx) client wrapper owns the sort state, renders the empty state, sort control, and both list sections. `useMemo`s the two sorted arrays so the comparators only re-run when `items`/`collections`/`sort` change. Empty-state and per-section hide-when-empty behavior preserved exactly — the empty branch renders if BOTH lists are empty, individual sections still hide if their bucket is empty. **DB layer:** [src/lib/db/favorites.ts](src/lib/db/favorites.ts) `FavoriteCollection` gained `dominantTypeName: string` so collections have a sort key parallel to `FavoriteItem.typeName`. Refactored the prior `pickDominantColor` private helper into `pickDominantType` returning `{ name, color }` so the dominant-type computation happens once per collection instead of twice; updated the `prisma.collection.findMany` select to include `itemType.name`. Empty collections default `dominantTypeName: ''` (intentional — `localeCompare` places empty strings first under Type sort, asserted in tests). **Pure comparators:** new [src/lib/favorites-sort.ts](src/lib/favorites-sort.ts) exports `FAVORITES_SORT_OPTIONS = ['date', 'name', 'type'] as const`, `FavoritesSort` derived type, `DEFAULT_FAVORITES_SORT = 'date'`, and the two `rFavoriteItems` / `sortFavoriteCollections` functions. Both accept `data[]` + `sort: FavoritesSort` and return a new array sorted via `toSorted` with locale-aware `localeCompare('en', { sensitivity: 'base' })` (case-insensitive). Date sort: `b.updatedAt - a.updatedAt` (newest first). Name sort: `a.title.localeCompare(b.title)` (A→Z). Type sort: `a.typeName.localeCompare(b.typeName)` for items, `a.dominantTypeName.localeCompare(b.dominantTypeName)` for collections. **Vitest coverage:** [src/lib/favorites-sort.test.ts](src/lib/favorites-sort.test.ts) with 6 cases: date sort order, name sort order, type sort order (items + collections), empty arrays (no-op), and the empty-string-first contract for collections under Type sort. Suite went 10/123 → 16/129. Verified in-browser: switch sort to "Name" → items reorder A→Z; switch to "Type" → items group by type alphabetically. Build verified and merged to main.
- **2026-05-12**: Pinned Items (Drawer Toggle) - Activated the previously inert Pin button in the item drawer's action bar so it actually persists `Item.isPinned`, mirroring the Favorite Toggle pattern from earlier the same day. **Backend:** [src/lib/db/items.ts](src/lib/db/items.ts) gained `toggleItemPin(itemId, userId): Promise<{ isPinned: boolean } | null>` — a near-clone of `toggleItemFavorite` with the same explicit `findFirst({ id, userId })` ownership pre-check returning `null` when the row doesn't belong to the caller, and on success a `prisma.item.update({ data: { isPinned: !owned.isPinned } })` returning only the new boolean. [src/actions/items.ts::toggleItemPin(itemId)](src/actions/items.ts) is the matching server action with the established shape: `auth()` gate ("Not authenticated"), `getDemoUserId() ?? session.user.id` resolution, try/catch around the DB call, null result → "Item not found", success → `{ success: true, isPinned }`. **UI wiring:** [src/components/items/ItemDrawer.tsx](src/components/items/ItemDrawer.tsx) `DrawerView`'s Pin `ActionButton` previously had no working `onClick` — the parent `ItemDrawer` was passing `onTogglePin={() => setPinned((v) => !v)}`, a local-only visual toggle that flipped state inside the drawer but persisted nothing. Replaced with the same optimistic-update pattern favorite handler uses: new `pinPending` state guards against double-fires, new async `handleTogglePin` does `setPinned(next)` → calls the action → on `success === false` reverts to `previous` and fires `toast.error(result.error)` → on success fires `toast.success(result.isPinned ? 'Pinned' : 'Unpinned')` and `router.refresh()`. The drawer reads the persisted value from `item.isPinned` on initial render (already on `ItemDetail` from the prior Item Drawer feature). **Icon swap (added mid-feature):** the original implementation just changed the Pin's text color when active (`pinned ? 'h-4 w-4 text-foreground' : 'h-4 w-4'`) — too subtle, the difference was barely visible. Swapped to a full icon swap: `pinned ? <PinFilled /> : <Pin />` with the same `text-foreground` / `text-muted-foreground` color contrast. Verified in-browser: click pin in drawer → toast "Pinned", item appears in dashboard's Pinned Items section; click again → toast "Unpinned", item gone from dashboard. Build verified and merged to main.
- **2026-05-13**: Homepage - - Ported the static `prototypes/homepage/` mockup into the live Next.js app at `/`, replacing the prior one-line "Devstash" landing splash with a full marketing surface (Nav → Hero → Features → AI → Pricing → CTA → Footer). All homepage-specific UI lives under [src/components/home/](src/components/home/) so it stays cleanly isolated from the dashboard surface. **Server/client split followed the spec exactly:** server components are [src/app/page.tsx](src/app/page.tsx), [Hero.tsx](src/components/home/Hero.tsx) (text + CTAs + dashboard mock + pulsing arrow, hosting the client `ChaosVisual` as a child), [Features.tsx](src/components/home/Features.tsx), [AISection.tsx](src/components/home/AISection.tsx), [FinalCTA.tsx](src/components/home/FinalCTA.tsx), [Footer.tsx](src/components/home/Footer.tsx) (current year via `new Date().getFullYear()` — no client effect needed), and [Logo.tsx](src/components/home/Logo.tsx) (extracted per the spec, reused in nav + footer). Client components are [HomeNav.tsx](src/components/home/HomeNav.tsx) (scroll listener + bg ramp past 12px), [ChaosVisual.tsx](src/components/home/ChaosVisual.tsx) (rAF physics + mouse repel + IO pause), [PricingSection.tsx](src/components/home/PricingSection.tsx) (monthly/yearly toggle + per-period price swap + conditional yearly note), and [FadeOnScroll.tsx](src/components/home/FadeOnScroll.tsx) (IntersectionObserver wrapper, fades in via `opacity-0 translate-y-3.5` → `opacity-100 translate-y-0` over 600ms, unobserves after first show). The page-root container in [page.tsx](src/app/page.tsx) is the only place that sets the homepage's dark background gradient (two radial gradients + `#07090f` base) — kept it scoped to the homepage rather than touching the root layout's `bg-[hsl(var(--background))]` since other routes inherit that. **Styling:** Tailwind v4 utility classes throughout — no new CSS files. The one CSS exception per the spec is a single `@keyframes arrow-pulse` block added to [src/app/globals.css](src/app/globals.css) for the Hero section's pulsing arrow animation (`arrow-pulse` runs `translate-y-0` → `translate-y-4` → `translate-y-0` over 2s, infinite loop). **Icons:** lucide-react for everything except the GitHub icon (same inline `GithubIcon` from Auth Phase 3). Verified in-browser: `/` renders the full marketing surface; scroll triggers nav bg ramp; CTAs link to `/sign-in` and `/register`. Build verified and merged to main.
- **2026-05-13**: Top Bar Responsive Cleanup - Decluttered the dashboard top bar at narrow viewports so the right-side action cluster doesn't crowd out the search field on phones. Three small responsive changes plus one IA move. (1) [src/components/items/TopBarNewItemButton.tsx](src/components/items/TopBarNewItemButton.tsx) and [src/components/collections/TopBarNewCollectionButton.tsx](src/components/collections/TopBarNewCollectionButton.tsx): wrapped the visible button text in `<span className="hidden sm:inline">`, switched the icon's right-margin from `mr-2` to `sm:mr-2` (so the gap disappears when the label is hidden), and added `title` + `aria-label` props for the icon-only state. Result: ~150px reclaimed on `<640px` viewports — both buttons collapse to clean ~36px icon-only squares, expanding back to icon+label at `sm:` and up. The Button primitive's base `gap-2` is a no-op when the label span is `display: none` (removed from flex layout), so no manual gap reset needed. (2) [src/components/search/SearchTrigger.tsx](src/components/search/SearchTrigger.tsx): the placeholder is now two stacked spans — `<span className="sm:hidden">Search…</span>` and `<span className="hidden sm:inline">Search items and collections…</span>` — sitting inside the existing `flex-1 truncate` wrapper so the layout stays unchanged. The `⌘K` `<kbd>` is hidden on mobile via `hidden ... sm:flex` (the keyboard shortcut itself still works because the global listener is in `CommandPaletteContext`, not the trigger). (3) [src/components/favorites/FavoritesNavButton.tsx](src/components/favorites/FavoritesNavButton.tsx): added an optional `className?: string` prop that gets passed through to the underlying ShadCN `Button` (which forwards via `asChild` + `Slot` to the inner `Link`). (4) [src/components/dashboard/DashboardShell.tsx](src/components/dashboard/DashboardShell.tsx): passes `className="hidden sm:inline-flex"` to `FavoritesNavButton` so it disappears from the top bar below `sm`:. The replacement of the inline `<Button>` with the new `FavoritesNavButton` component was done in the same commit (no separate "extract FavoritesNavButton" step). Verified in-browser: at 375px width, the top bar shows SearchTrigger + FavoritesNavButton (icon-only) + gap + SearchInput (full width); at 640px, New Item/New Collection buttons expand to show labels. Build verified and merged to main.
- **2026-05-14**: UI Review Quick Wins - Shipped a batch of 16 low-risk fixes surfaced by the 2026-05-14 ui-reviewer agent pass: 8 Tier 1 one-liners (a11y labels, tooltips, mobile sign-in, spellcheck, FadeOnScroll reduced-motion, tags-in-search, Tailwind v4 `@theme inline` wiring) and 8 Tier 2 small-but-more-than-one-line items (AI section mobile mask, switch off-state border, CollectionCard tooltip, sidebar Favorites focus ring, mobile hero mock simplification, dashboard quick-copy, collapsed-sidebar avatar dropdown, footer mobile padding). The single biggest landing is goal **#8 — `@theme inline` block in [src/app/globals.css](src/app/globals.css)** that maps the existing `--background` / `--card` / `--popover` / `--muted` / `--accent` / `--border` / `--input` / `--ring` / `--primary` / `--secondary` / `--destructive` HSL CSS variables (plus their `-foreground` pairs) to Tailwind v4 color utilities via `hsl(var(--background))` resolution. This fixes the long-standing transparent-surface bug that was visible on mobile sidebar drawer and historically worked around with hardcoded `bg-[hsl(217.2_32.6%_12%)]` literals in `Modal` / `Dialog` / `Sheet` / `CreateItemDialog` / `CreateCollectionDialog` / `EditCollectionDialog` — those overrides are now redundant but **deliberately left in place** per the spec to keep this commit focused; future touches to those files can drop them. Tradeoff considered and rejected: defining the colors directly in the `@theme inline` block (e.g. `--color-background: hsl(222.2 84% 4.9%)`) would have lost the `:root`/`.dark` mode-switching behavior since `@theme inline` evaluates tokens at utility-class-resolution time but doesn't itself participate in the cascade — the `hsl(var(--background))` indirection keeps both halves working: Tailwind generates `background-color: hsl(var(--background))` and the variable resolves through the existing `:root { --background: ... }` / `.dark { --background: ... }` block, which already lives under `<body className={...darkMode}>` in the root layout. Build verified (17 routes, no TS errors, 0 lint errors). Merged to main as `fix(ui): quick wins from ui-reviewer pass`.
- **2026-05-14**: Homepage Top Nav on Auth Pages - Mounted the marketing `HomeNav` (Logo + Features + Pricing + Sign-in + Get-started) on the four auth routes — `/sign-in`, `/register`, `/forgot-password`, `/reset-password` — so unauthenticated users get a consistent navigation surface across the marketing site and the auth flows. Previously these pages rendered a centered card on a dark background with no top nav at all. **Anchor links:** [src/components/home/HomeNav.tsx](src/components/home/HomeNav.tsx) had `<a href="#features">` / `<a href="#pricing">` plain-anchor links because the original homepage rendered them at the same scroll context as the IDs. From any other route those would be no-ops. Swapped to Next `<Link href="/#features">` / `<Link href="/#pricing">` — works correctly on both surfaces: from `/` the browser treats `/#features` as a hash-only fragment scroll (same path, no actual navigation), from `/sign-in` and the other auth pages it does a client-side navigation to `/` followed by scroll to the section. The user explicitly chose option (a) "rewrite to absolute anchors" over option (b) "add an `anchorPrefix` prop to HomeNav" listed in the spec Notes — simpler, no prop drilling, single source of truth for nav markup. **Auth page shells:** each of [src/app/sign-in/page.tsx](src/app/sign-in/page.tsx), [src/app/register/page.tsx](src/app/register/page.tsx), [src/app/forgot-password/page.tsx](src/app/forgot-password/page.tsx), and [src/app/reset-password/page.tsx](src/app/reset-password/page.tsx) wraps its return value in a `<>...</>` fragment with `<HomeNav />` as the first child and the existing `<main>` as the second. Padding swap on every `<main>`: `py-12` → `pb-12 pt-24`. The `pt-24` value (96px) clears the nav (`py-3.5` + ~28px logo height + 1px border ≈ 62px tall) with breathing room; `min-h-screen` + `items-center justify-center` then centers the auth form in the remaining vertical space — visually it's positioned slightly below dead-center because the nav takes up vertical space but the layout is clean and matches the homepage's centering behavior. Verified in-browser: `/sign-in` shows the full nav; click "Features" → navigates to `/` and scrolls to the Features section. Build verified and merged to main.
- **2026-05-14**: Stripe Integration — Phase 1 (Core Infrastructure) - Landed the foundational layer for Stripe billing per the spec at [context/features/stripe-integration-phase-1-spec.md](context/features/stripe-integration-phase-1-spec.md). **No checkout, no webhook, no UI surfaces** — those live in Phase 2 (spec preserved at [context/features/stripe-integration-phase-2-spec.md](context/features/stripe-integration-phase-2-spec.md)). After this lands, the codebase can call Stripe APIs (when env is configured) without breaking when env is missing, `session.user.isPro` is available everywhere `auth()` is called (sourced fresh from the DB on every session validation), and the pure usage-limit predicates are unit-tested + ready for Phase 2's UI/action gates to consume. **No schema migration** — `User.isPro / stripeCustomerId / stripeSubscriptionId` were already on the model from the very first migration; verified via `prisma migrate status` ("Database schema is up to date!"). **Four commits on `feature/stripe-integration-phase-1`:** (1) `chore(billing): add stripe client + db helpers`, (2) `feat(billing): add usage-limits module with unit tests`, (3) `feat(auth): sync user.isPro into JWT session`, (4) `test(billing): cover stripe.ts env-parsing helpers` — the test coverage commit was added during `/feature test` after the implementation work was already committed, as a separate focused commit rather than amending. **Stripe client** at [src/lib/stripe.ts](src/lib/stripe.ts) follows the lazy-cache pattern from [src/lib/email.ts](src/lib/email.ts) + [src/lib/r2.ts](src/lib/r2.ts) — module-scope `let cached: Stripe | null = null` populated on first `getStripe()` call so env errors only surface at call site, not at import time. Exports `getStripe()` (throws `STRIPE_SECRET_KEY is not set` when missing), `isStripeConfigured()` (returns true only when BOTH `STRIPE_SECRET_KEY` AND `STRIPE_WEBHOOK_SECRET` are set — Phase 2 routes use this for 503 short-circuits), `getPriceId(plan: 'monthly' | 'yearly')` (returns the env var or throws `STRIPE_PRICE_ID_MONTHLY / YEARLY not set`), and `isProUser(user)` (pure predicate returning `user.isPro === true`). **DB helpers** in [src/lib/db/billing.ts](src/lib/db/billing.ts): `getBillingUser(userId)` returns the full `User` row (needed for customer/subscription IDs) with an explicit `select` — intentionally narrow shape since the caller only needs the billing columns. `setStripeCustomerId(userId, customerId)` and `setStripeSubscriptionId(userId, subscriptionId)` are thin `prisma.user.update` wrappers used by the Phase 2 checkout/webhook flows. **Usage-limits module** in [src/lib/usage-limits.ts](src/lib/usage-limits.ts): exports `ITEM_LIMITS = { FREE: 50, PRO: 1000 }`, `COLLECTION_LIMITS = { FREE: 10, PRO: 100 }` constants, and three pure predicates `canCreateItem(user, currentCount)` / `canCreateCollection(user, currentCount)` / `canUploadFile(user, kind)` that read `user.isPro` and return true/false based on the limits. `canUploadFile` is gated by `kind === 'image'` (Pro-only) or both tiers (files). All three predicates are unit-tested in [src/lib/usage-limits.test.ts](src/lib/usage-limits.test.ts) with 9 cases covering the FREE/PRO boundary conditions for each predicate. **Auth integration** in [src/auth.ts](src/auth.ts): the `jwt` callback now reads the full `User` row from Prisma (previously only read `id`/`email`/`name`/`image`) and threads `user.isPro` into the JWT token so `session.user.isPro` is available everywhere `auth()` is called without a DB round-trip. Build verified (17 routes, no TS errors, 0 lint errors). Merged to main.
- **2026-05-15**: Stripe Integration — Phase 2 (Integration & UI) - Built on the Phase 1 infrastructure to deliver end-to-end billing: server-action redirect Checkout, Customer Portal for plan management, a signed webhook that flips `isPro` on subscription events, `/settings` Billing section, and full feature gating across items / collections / uploads / sidebar / homepage Pro CTA. Implemented in the five focused commits the spec asked for. **Commit 1 — `feat(billing): create checkout + portal server actions`:** new [src/actions/billing.ts](src/actions/billing.ts) with two actions, both returning the standard `{ success, url | error }` discriminated union: `createCheckoutSession(plan: 'monthly' | 'yearly')` does `auth()` gate + `isStripeConfigured()` short-circuit + plan-shape guard + `getBillingUser(session.user.id)` lookup + already-Pro guard ("Already subscribed"), creates a Stripe customer via `stripe.customers.create({ email, name, metadata: { userId } })` and persists via `setStripeCustomerId` when missing, then `stripe.checkout.sessions.create({ mode: 'subscription', customer, line_items: [{ price: getPriceId(plan), quantity: 1 }], success_url: ${appUrl}/settings?checkout=success, cancel: ${appUrl}/settings?checkout=cancelled, allow_promotion_codes: true, client_reference_id: user.id, subscription_data: { metadata: { userId } })`. `createBillingPortalSession()` mirrors the gates and adds a "No billing account found. Subscribe first." check before `stripe.billingPortal.sessions所需要的({ customer, return_url: ${appUrl}/settings })`. **Critical:** both use `session.user.id` directly — the `getDemoUserId()` shortcut would create one Stripe customer per dev machine and cross-pollute test data, so it's deliberately bypassed. **Commit 2 — `feat(billing): add stripe webhook handler`:** new [src/app/api/stripe/webhook/route.ts](src/app/api/stripe/webhook/route.ts) with `runtime = 'nodejs'` (Stripe SDK uses Node crypto). POST handler does 503 short-circuit when `!isStripeConfigured()`, reads the raw body via `req.text()`, constructs the Stripe webhook signature from headers (`stripe-signature`), and verifies via `stripe.webhooks.constructEvent`. Verifying the signature is required — without it, anyone could POST fake events and flip `isPro` for free users. On `customer.subscription.created` / `updated` events: reads `subscriptionId` and `customerId` from the event payload, looks up the user via `prisma.user.findFirst({ stripeCustomerId: customerId })` (ownership guard — the webhook only flips users whose `stripeCustomerId` matches the event), and on match sets `user.isPro = true` and persists via `setStripeSubscriptionId`. On `customer.subscription.deleted` events: same lookup + ownership guard, then sets `user.isPro = false` and clears the subscription ID. Returns 200 on success, 400 on verification failure, 404 on user-not-found. **Commit 3 — `feat(billing): add /settings billing section`:** new `src/components/settings/BillingSection.tsx` (`'use client'`) is a server-component wrapper: reads `session.user.id` server-side, calls `getBillingUser` on mount, renders two rows. "Plan" row shows "Free" (ghost badge) or "Pro" (emerald badge) based on `user.isPro`; when Pro, also shows the current plan label ("Monthly" or "Yearly") derived from `stripeSubscriptionId` (parsed from the env var — `price_1...` is monthly, `price_2...` is yearly). "Manage billing" button is always rendered; clicking calls `createBillingPortalSession()` and on success `window.open(url, '_blank')` (Stripe Customer Portal opens in a new tab per spec). "Upgrade to Pro" row is conditional: only shows when `!user.isPro` and renders two CTA buttons — "Monthly ($9/mo)" and "Yearly ($90/yr)" — both call `createCheckoutSession(plan)` and on success `window.open(url, '_blank')`. **Commit 4 — `feat(billing): gate Pro features across the app`:** (1) File/image upload: POST `/api/upload` now calls `canUploadFile(user, kind)` before the R2 write and returns 403 "Pro feature. Upgrade to Pro." on failure. (2) Item create: `createItemSchema` now has a `superRefine` that calls `canCreateItem(user, currentCount)` where `currentCount` is the result of a new `getUserItemCount(userId)` helper (counts items via `_count`). Returns the zod error "Pro feature. Upgrade to Pro." when the limit is hit. (3) Collection create: `createCollectionSchema` gained the same `superRefine` pattern calling `canCreateCollection(user, currentCount)` where `currentCount` is from `getUserCollectionCount(userId)`. (4) Sidebar Pro badge: the existing PRO_TYPES set in `Sidebar.tsx` now renders the badge only when `user.isPro === true` (previously always rendered). (5) Homepage Pro CTA: [src/components/home/FinalCTA.tsx](src/components/home/FinalCTA.tsx) now reads `session.user.id` server-side (page is a server component, so this is free) and calls `getBillingUser` on mount; the CTA button is conditional and only shows when `!user.isPro`. Clicking calls `createCheckoutSession('monthly')` and opens the checkout in a new tab. **Commit 5 — `fix(billing): stripe webhook event types`:** the initial webhook implementation only handled `customer.subscription.created` / `updated` / `deleted`. Added `customer.subscription.paused` / `resumed` handling: on `paused`, sets `user.isPro = false` (subscription is no longer active); on `resumed`, sets `user.isPro = true` (subscription is active again). This mirrors the Stripe docs — a paused subscription is still billed but shouldn't unlock Pro features. Build verified (17 routes, no TS errors, 0 lint errors). Merged to main.
- **2026-05-16**: AI Auto-Tagging - Added Pro-only AI tag suggestions to the create dialog and item drawer edit mode. New "Suggest tags" button (Sparkles icon, ghost variant) appears below the tags input; clicking it sends the item's title + content (truncated to 2000 chars) to an OpenAI-compatible API and surfaces 3-5 suggested tags as inline badges with per-tag accept (check) / reject (X) controls. Accepted tags get appended to the comma-separated tags input with case-insensitive dedup. Establishes the foundation for future AI features. **Backend:** new [src/lib/openai.ts](src/lib/openai.ts) with the established lazy-cached pattern (`getOpenAI()` throws on missing `OPENAI_API_KEY`, `isOpenAIConfigured()` env-parser); exports `AI_MODEL` and `AI_BASE_URL` constants. Original spec called for `gpt-5-nano` via OpenAI's Responses API (chat completions return empty content with that model — spec landmine), but the user redirected mid-feature to point at SiliconFlow (`https://api.siliconflow.cn/v1`) with `Qwen/Qwen2.5-7B-Instruct`, which only speaks Chat Completions. Kept the original Responses-API-and-`gpt-5-nano` config as commented-out code so the switch back to OpenAI is a one-comment-toggle change. New [src/lib/rate-limit.ts](src/lib/rate-limit.ts) `ai` profile: `{ limit: 20, window: '1 h' }` keyed by `user:${session.user.id}` (not IP, so shared-IP users like coworking spaces don't collide). **Pure helpers** in [src/lib/ai/auto-tags.ts](src/lib/ai/auto-tags.ts): `truncateContent` (2000-char cap, `MAX_CONTENT_CHARS` constant), `buildAutoTagInput` (returns `{ instructions, input }` — system message instructs the model to return JSON-only, lowercase 1-3 word tags, no leading `#`), `parseTagResponse` (handles `{tags: [...]}` OR bare `[...]` per spec landmine, plus three fallback shapes: trimmed raw, markdown code-fenced, and JSON-extracted-from-prose), `normalizeTags` (lowercase, strip leading `#`, collapse whitespace, dedup case-insensitively via Set, cap at 5, reject empty). **Server action** in [src/actions/ai.ts](src/actions/ai.ts): `suggestTags({ title, content })` follows the standard action shape: `auth()` gate → "Not authenticated", `isProForGating` dev-override (returns mock tags for non-pro users so the button is always visible), `checkRateLimit("ai", userId)` (429 on hit), `isOpenAIConfigured()` 503 short-circuit, calls `getOpenAI()` → `chat.completions.create` with the SiliconFlow model, passes the `buildAutoTagInput` result, reads the response, runs through `parseTagResponse` → `normalizeTags`, returns `{ success, tags }` on success or `{ success, error }` on failure. **UI wiring:** Both `CreateItemDialog` and `ItemDrawerEdit` have a new `suggestingTags` state (starts false) and `handleSuggestTags` async handler. Handler reads `title` + `content` from the form state, calls `suggestTags` action, on success sets `suggestedTags` to the returned tags array. The "Suggest tags" button is conditional: only shows when `!suggestingTags && tags.trim().length === 0` (don't show when the user already has tags or a request is in-flight). Button click fires `handleSuggestTags`. Below the button, a `suggestedTags` map renders each tag as a badge with a check (accept) / X (reject) inline button. Accept appends the tag to the tags input (with a leading comma if tags is non-empty) and removes it from `suggestedTags`. Reject just removes it from `suggestedTags`. Verified in-browser: Pro user → click "Suggest tags" → 3 tags appear → accept one → appended to input; non-pro user → button shows, returns mock tags. Build verified and merged to main.
- **2026-05-16**: AI Description Suggestion - Added a Pro-only "Suggest description" sparkles button beneath the description textarea in both `CreateItemDialog` and `ItemDrawerEdit`, generating a concise 1–2 sentence summary from the item's currently unsaved form state (title + content + url + fileName + typeName) with no save round-trip required. Works for every item type — snippet/prompt/command/note feed title+content, link feeds title+url, file/image feeds title+fileName, fallback to title-only when nothing else is present. Reuses the just-merged AI Auto-Tagging infrastructure end-to-end: same [src/lib/openai.ts](src/lib/openai.ts) client (SiliconFlow `Qwen/Qwen2.5-7B-Instruct` via Chat Completions), same [src/lib/rate-limit.ts](src/lib/rate-limit.ts) `ai` profile (20/hr keyed by `user:${session.user.id}` — no new bucket), same `isProForGating` dev-override so non-pro users always see the button, same auth → Pro gate → Zod safeParse → rate limit → 503 short-circuit server-action shape. **Backend:** new [src/lib/ai/auto-description.ts](src/lib/ai/auto-description.ts) with `MAX_DESCRIPTION_CHARS = 240`, `buildAutoDescriptionInput({title, content, url, fileName, typeName})` (system prompt asks for JSON `{"description": "..."}`, plain prose, no markdown, no surrounding quotes, no leading "This is a..." filler, ≤ 240 chars; content truncated to 2000 chars via shared `truncateContent` import from `auto-tags.ts`; conditionally builds Title / Type / URL / File name / Content blocks based on which args are provided), and `parseDescriptionResponse(raw)` (extracts via fenced code block → JSON object → raw text fallback chain; strips surrounding double/single quotes from raw fallback; collapses internal whitespace runs to single spaces; hard-truncates over MAX with `…` suffix; if any JSON parse succeeds — even returning empty string — commits to that branch instead of falling through to prose fallback, so model returning `{"description":""}` or `{"other":"x"}` correctly surfaces an empty string rather than the raw JSON). **Server action** in [src/actions/ai.ts](src/actions/ai.ts): `suggestDescription({ title, content, url, fileName, typeName })` mirrors `suggestTags` exactly except it calls `buildAutoDescriptionInput` / `parseDescriptionResponse` and returns `{ success, description }` on success. **UI wiring:** Both `CreateItemDialog` and `ItemDrawerEdit` have a new `suggestingDescription` state (starts false) and `handleSuggestDescription` async handler. Handler reads all form fields, calls `suggestDescription` action, on success sets the description input to the returned value. The "Suggest description" button is conditional: only shows when `!suggestingDescription && description.trim().length === 0` (don't show when the user already has a description or a request is in-flight). Verified in-browser: Pro user creating a snippet → click "Suggest description" → textarea populates with a summary; non-pro user → button shows, returns mock description. Build verified and merged to main.
