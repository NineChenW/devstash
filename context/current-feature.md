# Update Rules

!!! Read this before update. !!!
Do not violate any following rules:

1. Please keep all the comments stay the same.
2. Do not delete the existing history records.
3. Only add a new history record after a feature is stetted completed.
4. Fill the current feature area with the current active feature title.
5. Update status to "In Progress" when starting a new feature.
6. Update status to "Completed" when finishing a feature.
7. Update goals section with current feature requirements.
8. Update notes section with current feature references.

# Current Feature

<!--Feature Name-->

Codebase Audit Quick Wins (2026-05-05)

## Status

<!--Not Started|In Progress|Completed-->

In Progress

## Goals

<!--Goals & requirements-->

Land seven low-risk fixes from the 2026-05-05 code audit (`docs/audit-results/CODEBASE_AUDIT_2026-05-05.md`) in a single cleanup branch. Each is small, independent, and verifiable via `npm run test:run` + `npm run build`. No new features.

1. **Wire up middleware** — create `src/middleware.ts` with `export { proxy as middleware, config } from './proxy'`. The existing `proxy.ts` is currently dead code because Next.js only loads middleware from a file named `middleware.ts`. This activates the `/dashboard` auth gate that was already written but never running.

2. **Validate `callbackUrl` in sign-in** — `src/app/sign-in/SignInForm.tsx:20`. The current `searchParams.get('callbackUrl') || '/dashboard'` is fed directly into `router.push()` after credentials sign-in, so `/sign-in?callbackUrl=https://evil.com` redirects off-site after login. Add an origin-check helper that rejects any non-relative URL and falls back to `/dashboard`. Use `new URL(raw, window.location.origin)` and compare `url.origin` to the page origin.

3. **Assert R2 key shape in file proxy** — `src/app/api/files/[...key]/route.ts:21-23`. After joining and decoding the path segments, reject keys that don't start with `users/` or contain `..`:
   ```ts
   if (!key || key.includes('..') || !key.startsWith('users/')) {
     return new Response('Not found', { status: 404 })
   }
   ```
   The DB ownership check already gates real access; this is defense in depth that costs nothing.

4. **Fix `sanitizeFileName` slice direction** — `src/lib/file-constraints.ts:108`. The current `name.trim().slice(-120)` keeps the *tail* 120 chars of long filenames, throwing away the meaningful start of the name. Change to `.slice(0, 120)`. Update or extend the existing `sanitizeFileName` test in `src/lib/file-constraints.test.ts` to cover the long-name case so this can't regress.

5. **Dedupe `formatBytes`** — `src/components/items/ItemDrawer.tsx:639-643` defines a byte-for-byte copy of `formatBytes` from `src/lib/file-constraints.ts:99-103`. Delete the local copy and import from `@/lib/file-constraints`.

6. **Bound unbounded `findMany` queries** — `src/lib/db/items.ts`. Add `take: 200` to both `getItemsByType` and `getPinnedItems` as a guardrail until cursor pagination lands. Add a short `// TODO: replace with cursor pagination` comment on each so the limit is visible when pagination work begins. Has zero effect on current users (demo has ~17 items).

7. **Fix sidebar collapse flash** — `src/components/dashboard/DashboardShell.tsx:27-39`. The component starts with `collapsed: false`, then a `useEffect` reads `localStorage` and may flip it to `true`, causing a visible layout shift on first render. Replace with a `useState` lazy initializer that reads `localStorage` synchronously (guarded for SSR with `typeof window === 'undefined'` and a `try/catch` around `JSON.parse`). Remove the now-redundant read-only `useEffect`. Keep the existing `useEffect` that *writes* to `localStorage` on toggle.

**Out of scope for this feature** (deliberately deferred):
- The unused `id` prop on `ItemCard` (audit finding #14) — kept for now in case it becomes useful when the drawer opens via URL.
- Larger refactors flagged in the audit (extract `TYPES_WITH_*` sets, combine ownership-check + update round-trips, refactor `getRecentCollections`, rate-limit + storage cap on `/api/upload`, gate `/items/*` in the proxy matcher). These are real but each warrants its own change.

## Notes

<!--Any extra notes-->

- Audit report: `docs/audit-results/CODEBASE_AUDIT_2026-05-05.md`.
- Branch name: `fix/audit-quick-wins-2026-05-05` (per `context/ai-interaction.md` workflow).
- Verification: `npm run test:run` + `npm run build` after the changes; for #2/#3 also confirm the rejection paths via curl with `--noproxy '*'`.
- Per `coding-standards.md`, only #4 needs a Vitest case (it's a pure utility); the others are component/route changes which are out of test scope.
- Per `ai-interaction.md`, ask before committing — wait for build to pass first.
- **Goal #1 finding:** the audit's premise was wrong for Next.js 16. Next.js 16 renamed the middleware file to `proxy.ts` (see https://nextjs.org/docs/messages/middleware-to-proxy), so `src/proxy.ts` is already auto-loaded — it was never dead code. Adding `src/middleware.ts` made the build fail with `Both middleware file and proxy file are detected. Please use proxy.ts only.` Reverted by deleting `src/middleware.ts`. The `/dashboard` auth gate is verified active (curl `/dashboard` → 302 → `/sign-in?callbackUrl=/dashboard`); build output now shows `ƒ Proxy (Middleware)`.


## History

<!--
Keep this updated.
Earliest to latest.
-->

- **2026-04-14**: Initial Next.js and Tailwind CSS v4 setup - Created project structure, coding standards, and AI interaction guidelines. Committed with message "chore: initial next.js and tailwind setup".
- **2026-04-15**: Dashboard UI Phase 1 implementation - Initialized ShadCN UI components, created dashboard route at /dashboard, implemented dark mode by default, added top bar with search and new item button, created sidebar and main area placeholders.
- **2026-04-16**: Dashboard UI Phase 2 implementation - Implemented collapsible sidebar with item types links, favorite collections, most recent collections, user avatar area, drawer icon for mobile/desktop toggle, fixed hydration error with mounted state, added localStorage persistence for sidebar state.
- **2026-04-16**: Dashboard UI Phase 2 completion - Created Sidebar component with collapsible functionality, SidebarDrawer for mobile view, integrated with dashboard page, added localStorage persistence for sidebar collapsed state. Build verified successfully.
- **2026-04-16**: Bug fixes and sidebar improvements - Fixed toggle icon not changing between collapsed/expanded states, added colored left border accent to PinnedItem to match CollectionCard, made Collections section in sidebar collapsible with chevron animation, fixed sidebar not rendering after browser back navigation (removed mounted guard from sidebar render).
- **2026-04-16**: Dashboard UI Phase 3 completion - Implemented stats cards (total items, collections, favorites), recent collections section, pinned items section, and recent items section. Created DashboardShell and StatsCard components, RecentItem component. Updated mock data for development. Build verified and merged to main.
- **2026-04-17**: Neon PostgreSQL + Prisma Setup - Configured Prisma 7 with Neon serverless adapter, created initial migration with all data models, added seed script with system item types and demo data, added database test script. Build verified and merged to main.
- **2026-04-17**: Seed Data Population - Overwrote seed file with spec-compliant data, created demo user (demo@devstash.io), seeded all 7 system item types, created 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources), populated collections with realistic items. Build verified and merged to main.
- **2026-04-17**: Dashboard Collections - Created src/lib/db/collections.ts with data fetching functions, replaced mock collection data in dashboard with real Neon database queries via Prisma, collection card border color derived from most-used content type, type icons shown per collection, stats fetched from database. Fixed pre-existing seed.ts type error. Build verified.
- **2026-04-17**: Dashboard Items - Created src/lib/db/items.ts with getPinnedItems and getRecentItems functions, replaced mock pinned/recent item data in dashboard with real Neon database queries via Prisma. Updated PinnedItem and RecentItem components to receive type icon/color/name directly (removed mock-data dependency) and added type tag pill. Pinned section hides when empty. Build verified.
- **2026-04-17**: Stats & Sidebar - Added getSystemItemTypesWithCounts to src/lib/db/items.ts returning system item types in fixed order (snippet, prompt, command, note, file, image, link) with per-user counts. Rewired Sidebar to consume real DB data via props (itemTypes, collections) threaded through DashboardShell and SidebarDrawer; removed mockItemTypes/mockCollections usage. System type links point to /items/[name]s. Favorites keep the star icon; recents show a colored dot using the collection's dominant item-type color. Added "View all collections" link to /collections. Build verified.
- **2026-04-18**: Add Pro Badge to Sidebar - Installed ShadCN Badge component at src/components/ui/badge.tsx. Added PRO_TYPES set (file, image) in Sidebar.tsx and rendered a small outlined PRO badge between the item-type title and the count for those types; count pinned right via ml-auto, badge hidden when sidebar is collapsed. Covers mobile drawer automatically since SidebarDrawer wraps Sidebar. Build verified and merged to main.
- **2026-04-18**: Codebase Quick Wins Cleanup - Applied low-risk fixes from the code-scanner audit. Extracted shared iconMap to src/lib/icon-map.ts (removed duplicates from Sidebar, CollectionCard, PinnedItem, RecentItem). Dropped unnecessary 'use client' from PinnedItem and RecentItem. Added take: 20 bound to getRecentCollections. Guarded JSON.parse in DashboardShell against malformed localStorage. Removed unused mock exports (kept mockUser until auth lands). Dropped redundant @@index([email]) and @@index([name]) via Prisma migration drop_redundant_indexes. Added dashboard loading.tsx skeleton and error.tsx boundary. Build verified and merged to main.
- **2026-04-19**: Auth Phase 1 - NextAuth + GitHub Provider - Installed next-auth@beta and @auth/prisma-adapter. Split config: src/auth.config.ts (edge-safe GitHub provider + jwt/session callbacks populating user.id) and src/auth.ts (PrismaAdapter + session strategy 'jwt'). Added /api/auth/[...nextauth] route handler re-exporting handlers.GET/POST. Created src/proxy.ts exporting `proxy = auth(...)` that redirects unauthenticated visitors to `/dashboard/*` to `/api/auth/signin?callbackUrl=...`. Extended Session.user with id via src/types/next-auth.d.ts. Build verified; /dashboard redirect confirmed via curl; default NextAuth sign-in page renders with GitHub button. Merged to main.
- **2026-04-20**: Auth Phase 2 - Credentials (Email/Password) Provider - Added Credentials provider alongside GitHub using the split-config pattern: src/auth.config.ts declares a Credentials placeholder with `authorize: () => null` (edge-safe); src/auth.ts filters the placeholder by provider id and re-registers Credentials with a bcryptjs-backed authorize that looks up the user via Prisma and compares against the hashed password. Added POST /api/auth/register route that validates name/email/password/confirmPassword, rejects duplicate emails (409), enforces min 8-char passwords, hashes with bcryptjs (cost 10), and returns {success, user}. No migration needed — password column already exists from the earlier add_user_password migration. Verified via curl: register happy-path (201), duplicate (409), mismatched pw (400), short pw (400); credentials signin sets session cookie and /dashboard returns 200; wrong password redirects to /signin?error=CredentialsSignin with null session; GitHub provider still listed at /api/auth/providers; unauth /dashboard still redirects. Build verified and merged to main.
- **2026-04-20**: Auth Phase 3 - Custom Sign-in / Register UI + Sidebar User - Added `pages: { signIn: "/sign-in" }` to src/auth.config.ts and updated src/proxy.ts to redirect unauthenticated `/dashboard/*` visitors to `/sign-in?callbackUrl=...`. Built custom `/sign-in` (credentials form + GitHub button via `signIn()`, honors `callbackUrl`, redirects signed-in users) and `/register` (name/email/password/confirm with client-side validation; posts to `/api/auth/register`; redirects to `/sign-in?registered=1`). Added `/profile` placeholder gated by `auth()`. Created reusable `UserAvatar` (image-or-initials fallback, cap 2 chars, `?` when empty) and `SidebarUser` client component with click-outside/Escape-close dropup containing a `signOut({ callbackUrl: "/sign-in" })` action; avatar wrapped in a Link to `/profile`. Threaded session data from `auth()` through DashboardShell → Sidebar / SidebarDrawer props; deleted src/lib/mock-data.ts. Added inline `GithubIcon` (lucide-react has no `Github` export in this version). Installed `sonner`, mounted `<Toaster>` in root layout, and fire a deduped success toast on `/sign-in` when `?registered=1` is present (then scrub the param from the URL). Kept `next.config.ts` `devIndicators: false`. Verified in-browser: sign-out clears session and lands on /sign-in; avatar → /profile works; register → /sign-in with toast → new user signs in and sees "P3" initials in sidebar. Build verified and merged to main.
- **2026-04-21**: Email Verification on Register (Resend) - Installed `resend` and built verification on top of the existing `VerificationToken` table (24h TTL; no migration). Added src/lib/email.ts (Resend client + branded HTML/text template; from-address and app URL driven by EMAIL_FROM / APP_URL / NEXT_PUBLIC_APP_URL / AUTH_URL) and src/lib/verification-token.ts (createVerificationToken replaces prior rows for the identifier via deleteMany then inserts a fresh 32-byte hex token; consumeVerificationToken returns `{ok,email}` or `{ok:false, reason:"not-found"|"expired"}` and always deletes the row). POST /api/auth/register now issues a token and sends the email after user creation inside a try/catch so send failures are logged but non-fatal. Added GET /api/auth/verify (consumes token, sets `User.emailVerified`, redirects to `/sign-in?verified=1` or `/sign-in?verify=expired|invalid|missing`) and POST /api/auth/verify/resend (uniform 200 response to avoid email enumeration; only actually sends if a matching unverified user exists). src/auth.ts adds an `EmailNotVerifiedError extends CredentialsSignin` (code `"EmailNotVerified"`) thrown in `authorize` when the password is valid but `emailVerified` is null; GitHub OAuth users are unaffected. SignInForm handles `res.code === "EmailNotVerified"` by showing a "Resend verification email" button; updated toasts for `?registered=1` (new copy prompts verification), `?verified=1`, and `?verify=expired|invalid|missing`. Also added scripts/cleanup-users.ts — deletes every user except demo@devstash.io (relying on User cascade for accounts/sessions/items/collections/custom itemTypes) plus stale VerificationToken rows, with a production-URL guard and interactive `yes` confirmation (skippable via `--yes`). Build verified and merged to main.
- **2026-04-21**: Toggle Email Verification Flag - Added a runtime kill-switch for the Resend verification flow, prompted by Resend sandbox mode only delivering to the account owner's own address. New `src/lib/features.ts` exports `isEmailVerificationEnabled()` which returns true only when `EMAIL_VERIFICATION_ENABLED` is `"true"`/`"1"` (case-insensitive, trimmed); any other value — including unset — disables verification. POST /api/auth/register now branches on the flag: when off, the user is created with `emailVerified: new Date()` and no token is issued / email sent; when on, behavior is identical to the prior feature. src/auth.ts gates the `EmailNotVerifiedError` throw behind the same flag so credentials sign-in succeeds for unverified users when off; GitHub OAuth is untouched. POST /api/auth/verify/resend short-circuits with a uniform 200 when off (preserves the enumeration-safe contract, skips the DB hit); GET /api/auth/verify is left functional so legacy tokens issued before the flip can still be consumed harmlessly. `/sign-in/page.tsx` now reads the flag server-side and threads it into `SignInForm` as `emailVerificationEnabled`; the `?registered=1` toast copy switches to "Account created — you can now sign in." when off so we don't promise an email that's not coming. The resend CTA remains naturally gated (only fires on `EmailNotVerified`, which can't happen when off). `.env.example` documents the new var with a "false" default (gitignored in this repo, so the file is edited locally only). Build verified and merged to main.
- **2026-04-21**: Forgot Password - Added a full password reset flow on top of the existing `VerificationToken` table (no migration) using a `pwreset:<email>` identifier namespace so reset tokens can coexist with email verification tokens in the same row set. Extended `src/lib/verification-token.ts` with `createPasswordResetToken` (1h TTL vs 24h for verification; deleteMany-then-insert replaces prior reset rows for the same email), `checkPasswordResetToken` (validates without consuming — used by the `/reset-password` server page to decide whether to render the form or an error state), and `consumePasswordResetToken` (single-use, delete-on-consume, returns `{ok,email}` or `{ok:false, reason:"not-found"|"expired"}`). Hardened `consumeVerificationToken` to skip any row whose identifier starts with `pwreset:` so GET `/api/auth/verify` can't accidentally invalidate a reset token — verified: passing a reset token to verify redirects to `?verify=invalid` and the DB row is untouched. Added `sendPasswordResetEmail` to `src/lib/email.ts` with a branded template linking to `/reset-password?token=...` and 1h expiry copy. POST `/api/auth/forgot-password` is enumeration-safe (uniform 200 regardless of match) and only issues a token + sends mail when the user exists *and* has a password — GitHub-only accounts get the silent 200, no token, no email. POST `/api/auth/reset-password` validates the token, enforces 8-char minimum + match (same contract as register), bcrypt-hashes at cost 10, updates `User.password`, and deletes the token; returns `{error, code:"expired"|"invalid"|"missing"}` on failures so the client can link to `/forgot-password`. Not gated by `EMAIL_VERIFICATION_ENABLED` — reset must work regardless of that flag. Built `/forgot-password` (email form → "check your inbox" success state, doesn't leak whether the email exists) and `/reset-password` (server component validates the token before rendering; `missing`/`expired`/`invalid` error cards include a "Request a new link" CTA, valid token shows new-password + confirm form prefilled with the user's email). `SignInForm.tsx` gained a "Forgot password?" link next to the password label and handles `?reset=1` with a success toast that is scrubbed from the URL on display. Verified via curl + Neon: register → forgot-password creates 1 pwreset token with 1h TTL; reset-password mismatch 400, short 400, missing 400, happy 200, replay 400 (invalid), garbage 400 (invalid); new password signs in (session issued, /dashboard 200), old password rejected with CredentialsSignin + null session; /reset-password renders the right state for missing / invalid / valid tokens; forgot-password returns uniform 200 for unknown emails and for GitHub-only users. Heads-up: Resend sandbox still only delivers to the owner's address (gordonwang2023@gmail.com) — send failures are caught + logged so the API contract stays uniform. Build verified and merged to main.

- **2026-04-22**: Profile Page - Replaced the Auth Phase 3 `/profile` placeholder with a full profile view. Added `src/lib/db/profile.ts` with `getProfileUser` (returns id/name/email/image/createdAt plus a `hasPassword` boolean derived from `User.password`) and `getProfileStats` (totalItems, totalCollections, and a `typeCounts` array in fixed order snippet→prompt→command→note→file→image→link with per-user item counts). Rebuilt `src/app/profile/page.tsx` as a server component gated by `auth()` (redirects to `/sign-in?callbackUrl=/profile` when unauthenticated — inline check, since proxy.ts only matches `/dashboard/*`): header with `UserAvatar`, name/email, and "Member since" from `createdAt` via `Intl.DateTimeFormat`; Usage section reusing `StatsCard` for Total Items / Collections plus a card listing items by type using the shared `iconMap`; Account section with a conditional "Change password" row (rendered only when `hasPassword`) and an always-present "Delete account" row. Added POST `/api/auth/change-password` (session-gated via `auth()`; 400 on OAuth-only accounts with no password, 400 on wrong current password, 400 on mismatch or <8 chars, 401 when unauthenticated; bcrypt-verifies current and hashes new at cost 10) and DELETE `/api/account` (session-gated; deletes the user row, relying on the existing cascade relations for items/collections/custom itemTypes/accounts/sessions; 401 when unauthenticated). Built a reusable `src/components/ui/modal.tsx` (Escape-close, body-scroll lock, explicit `hsl(217.2 32.6% 12%)` surface + lighter border because this project doesn't wire `@theme` in Tailwind v4 so `bg-popover` renders transparent against the backdrop) plus `ChangePasswordDialog` (current/new/confirm with client+server validation, sonner success toast on update) and `DeleteAccountDialog` (confirm button disabled until the user types `DELETE` — case-sensitive; on success calls `signOut({ callbackUrl: "/sign-in" })`). Verified in browser with the demo user: profile renders correct totals (18 items / 5 collections) and the full type breakdown, wrong current password returns 400 with inline error, happy path closes the dialog + fires the toast (flipped demo pw and reverted to keep seed state clean), delete dialog's confirm button correctly stays disabled for lowercase `delete` and empty input. Build verified and merged to main.

- **2026-04-25**: Rate Limiting for Auth - Added Upstash sliding-window rate limiting on the auth surface to mitigate brute force, credential stuffing, and email-abuse. Installed `@upstash/ratelimit` + `@upstash/redis` and built `src/lib/rate-limit.ts` as the single utility: lazy `getRedis()` reads `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` at first use and memoizes `null` when unset; `getLimiter(profile)` builds one `Ratelimit` per profile (`login`, `register`, `forgot-password`, `reset-password`, `resend-verification`) with `Ratelimit.slidingWindow(limit, window)` and a `rl:<profile>` Redis prefix so buckets don't collide across endpoints. `checkRateLimit(profile, identifier)` returns `{ success, remaining, reset, limit }` and is hard-wired to fail open: missing creds short-circuit to `success:true`, and `limiter.limit()` errors are caught + logged so Upstash outages can't lock everyone out. Helpers: `getRequestIp(req)` parses `x-forwarded-for` (first hop) with `x-real-ip` and `127.0.0.1` fallbacks; `buildRateLimitKey(ip, extra)` lowercases the email tail; `minutesUntil(reset)` floors at 1; `rateLimitResponse(result)` returns 429 with `{ error: "Too many attempts. Please try again in N minutes.", code: "rate_limited" }` plus `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` headers and grammatical singular/plural copy. Wired the limiter into POST `/api/auth/register` (3/hr by IP), POST `/api/auth/forgot-password` (3/hr by IP), POST `/api/auth/reset-password` (5/15min by IP), POST `/api/auth/verify/resend` (3/15min by IP+email — applied AFTER body parse + email validation so 400s for empty bodies fire first and garbage payloads can't poison the bucket). For credentials login, NextAuth owns `/api/auth/callback/credentials` so the guard lives inside `src/auth.ts` `authorize`: a new `RateLimitedError extends CredentialsSignin` (code `"RateLimited"`) is thrown when `checkRateLimit("login", ip+normalizedEmail)` fails, before the bcrypt compare; reads the request via `authorize`'s second argument. Frontend forms surface 429s as `sonner` toasts plus inline error text: `RegisterForm` / `ForgotPasswordForm` / `ResetPasswordForm` branch on `res.status === 429` and read `data.error` from the JSON; `SignInForm` recognizes `res.code === "RateLimited"` from `signIn('credentials')` and the resend-verification 429 from the same form. `.env.example` (gitignored locally) documents `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` with a fail-open note. Verified end-to-end against real Upstash: `register` 4th hit returns 429 with `Retry-After: 2127`, `X-RateLimit-Limit: 3`, `X-RateLimit-Remaining: 0` and the friendly minutes message; `forgot-password` keeps an independent bucket (200 after register exhausted); credentials login throws `CredentialsSignin: Too many sign-in attempts. Try again in 1 minute.` on the 6th attempt; build passes (`next build`, 17 routes). Heads-up while debugging: an earlier 4.5s consistent 500 across all routes turned out to be Privoxy intercepting localhost curl — not the app — `--noproxy '*'` confirms the dev server is healthy. Build verified and merged to main.

- **2026-04-25**: Fix GitHub OAuth Redirect - Fixed the two-click GitHub sign-in bug where the first click authenticated successfully but failed to redirect to `/dashboard` (page bounced back to `/sign-in`). Root cause was the unreliable client-side redirect from `signIn('github', { callbackUrl })` in `next-auth/react` when the OAuth round-trip races with router state. Switched to the recommended NextAuth v5 pattern: added `src/actions/auth.ts` exporting a `'use server'` `signInWithGitHub` action that calls `signIn('github', { redirectTo: '/dashboard' })` from `@/auth` (server-side `signIn`, not the client export). Updated `src/app/sign-in/SignInForm.tsx` (note: real path — spec referenced a non-existent `src/components/auth/sign-in-form.tsx`) to import `signInWithGitHub` and replace the GitHub `<Button onClick={() => signIn('github', { callbackUrl })}>` with `<form action={signInWithGitHub}><Button type="submit" variant="outline" className="w-full">…</Button></form>`. Spec mentioned removing `isGitHubLoading` state and `handleGitHubSignIn` function — neither existed in this codebase (only an inline arrow handler), so nothing extra to delete. Credentials login left untouched — it uses `redirect: false` + manual `router.push(callbackUrl)` and works correctly. Tradeoff: `redirectTo` is hardcoded to `/dashboard` per spec, so the prior `?callbackUrl=...` preservation for GitHub sign-ins (e.g. `/dashboard/items` → `/sign-in?callbackUrl=/dashboard/items` → GitHub → back to `/dashboard/items`) is lost; only credentials login still honors `callbackUrl`. No `SessionProvider` needed. Build verified (`next build`, 17 routes, no TS errors). Merged to main.

- **2026-04-26**: Items List View - Added a dynamic `/items/[type]` route that lists items filtered by item type (e.g. `/items/snippets`, `/items/notes`, `/items/links`, `/items/files`). The URL segment is plural and the DB stores singular type names, so the page validates the trailing `s` suffix (404s if absent — `/items/snippet` → 404) and slices it off to look up the type. Extended `src/lib/db/items.ts` with `getItemsByType(userId, typeName)` which finds the type via `prisma.itemType.findFirst({ where: { name, OR: [{ isSystem: true }, { userId }] } })` (covers system + user-owned custom types), returns `null` when the type doesn't exist (the page calls `notFound()`), and otherwise returns `{ type: { name, icon, color }, items }` ordered `isPinned desc, updatedAt desc` with each item carrying `typeIcon/typeColor/typeName`, `isFavorite`, `isPinned`, `tags`, and `createdAt`. New shared types `ItemListItem extends DashboardItem` (adds `isPinned`) and `ItemsByTypeResult` exported from the same module. Created `src/components/items/ItemCard.tsx` — server component (no `'use client'`), 3px left border in the type's color via `style={{ borderLeftColor, borderLeftWidth: '3px' }}` (Tailwind v4 + arbitrary hex from DB, so inline style not class), an icon tile filled with `${color}20`, type pill, optional pin/star indicators, line-clamp-2 description, tag chips, and date. Built `src/app/items/[type]/page.tsx` as a server component using the same DashboardShell pattern as `/dashboard` (sidebar + topbar shell, sidebar data via `getRecentCollections` + `getSystemItemTypesWithCounts`, demo user via `getDemoUserId` for parity with the dashboard — `session?.user` only feeds the sidebar avatar; the per-user-data plumbing is a wider refactor for later). Reads `params` as `Promise<{ type: string }>` per Next.js 16 (await it), grid is `grid-cols-1 gap-4 md:grid-cols-2`, header shows the type icon + plural name + item count with singular/plural copy, and renders an empty-state card when `items.length === 0`. Verified in browser via curl (`--noproxy '*'`): `/items/snippets|notes|links|files` → 200 with the right heading, type-colored borders (snippet blue `#3b82f6`, etc.), and md:2-col grid; `/items/bogus` → 404; `/items/snippet` (singular) → 404. Build passes (`next build`, 18 routes). Merged to main.

- **2026-04-27**: Vitest Setup - Added unit testing scaffolding focused on server actions and pure utility/library code (no component or DB-touching tests). Created `vitest.config.ts` using the `node` test environment with `vite-tsconfig-paths` for `@/*` alias resolution and `include: ["src/**/*.test.ts"]`. Added `test` (watch) and `test:run` (one-shot) npm scripts; vitest 4 + vite-tsconfig-paths were already in `devDependencies` from a prior install. Wrote co-located sample tests: `src/lib/utils.test.ts` covers `cn` (clsx semantics + tailwind-merge conflict resolution), `src/lib/features.test.ts` covers `isEmailVerificationEnabled` env-var parsing (true/1 case-insensitive + whitespace, false otherwise — wraps each test in beforeEach/afterEach to restore the original env var so the suite is order-safe), and `src/lib/rate-limit.test.ts` covers the three pure helpers `getRequestIp` (x-forwarded-for first hop, x-real-ip fallback, 127.0.0.1 fallback, comma-only edge case), `buildRateLimitKey` (lowercase + trim of email tail, 'unknown' fallback for empty IP), and `minutesUntil` (uses `vi.useFakeTimers()` + `vi.setSystemTime()` for deterministic Date.now math; floor-at-1 + ceil partial minutes). Updated `context/ai-interaction.md` workflow step 4 to require Vitest tests for new server actions / lib code and `npm run test:run` before commit, and added a Testing section to `context/coding-standards.md` (scope, location convention, "no Prisma mocking" guidance, commands). Skipped tests for `src/actions/auth.ts::signInWithGitHub` (thin NextAuth wrapper, no branching) and DB-backed modules (real-DB integration tests are a separate piece of work). Verified `npm run test:run` (3 files / 22 tests pass in ~210ms) and `npm run build` (17 routes, no TS errors). Heads-up: vitest 4 emits an info notice that vite-tsconfig-paths could be replaced with the native `resolve.tsconfigPaths: true` option; left the plugin in per spec. Merged to main.

- **2026-04-28**: Items List 3-Column Grid - Updated the grid in `src/app/items/[type]/page.tsx` from `grid-cols-1 gap-4 md:grid-cols-2` to `grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3` so the items list shows three cards per row on large screens (≥1024px) while remaining responsive (1 col mobile, 2 cols ≥768px). One-line Tailwind class change in the listing page; ItemCard, data fetching, and empty-state branch left untouched. No server actions or utility/library code added, so no new Vitest tests warranted under our scope; existing suite re-run as a sanity check (3 files / 22 tests pass). `npm run build` passes (17 routes, no TS errors). Merged to main.

- **2026-04-30**: Item Drawer (View + Inline Edit) - Right-side slide-in drawer that opens when an item card is clicked on `/dashboard` or `/items/[type]`, replacing the need for a separate detail page. Built a custom `src/components/ui/sheet.tsx` (shadcn-style — Sheet wasn't pre-installed, so wrote a minimal version with backdrop, slide-in transition, Escape close, and body-scroll lock) instead of pulling in the shadcn CLI's. Client wrappers `ItemDrawerProvider` (owns `openId` state, renders the drawer at the root of `<main>`) and `ItemTrigger` (button-as-card) so the server-rendered cards on the dashboard and items list can open the drawer without a page navigation; `DashboardShell` was changed to wrap `children` in `<ItemDrawerProvider>` so both pages get drawer support automatically, and the dashboard's Pinned/Recent + items list cards are now wrapped in `<ItemTrigger itemId={item.id}>`. Detail fetched on click via new `GET /api/items/[id]` (auth + ownership scoped through `getDemoUserId() ?? session.user.id` for parity with the dashboard's demo-user pattern; 401 unauthenticated, 404 when not found or not owned), backed by a new `getItemDetail(itemId, userId)` query in `src/lib/db/items.ts` that returns full `ItemDetail` (content/url/file/language/tags/collections/dates). View mode shows the item's icon tile + title + type pill, an action bar with Favorite/Pin (visual toggles, no persistence yet) / Copy (clipboard with sonner toast) / Edit (flips to edit mode) / Delete (stubbed to "coming soon" toast), description, content preview (link → external anchor, file → name+size, text → `<pre><code>` block), tag chips, collections, and Created/Updated dates. Skeleton renders during the in-flight fetch. Edit mode flips the same drawer in place (no separate page/modal): action bar swaps for Save/Cancel, controlled inputs for title (required, autofocused), description, type-conditional `content` textarea (snippet/prompt/command/note), `language` input (snippet/command), `url` input (link), tags as comma-separated string, with the item type, collections, and Created/Updated shown read-only in a Details block. Save is disabled while the trimmed title is empty. New `updateItem` server action in `src/actions/items.ts` (returns `{ success, data | error, fieldErrors? }`): `auth()` gate, Zod `safeParse` against `updateItemSchema` (factored into `src/lib/validations/items.ts` because Next.js requires `'use server'` files to export only async functions, and the test imports the schema directly), tag dedup via `Array.from(new Set(...))`, then a new `updateItem` query in `src/lib/db/items.ts` that does an ownership pre-check and updates with `tags: { set: [], connectOrCreate: [...] }` (Prisma's clean tag-swap pattern — disconnect all then connect-or-create by name) and returns the refreshed `ItemDetail` so the client doesn't need a second fetch. After save, drawer flips back to view with the new data and the page calls `router.refresh()` so card lists reflect changes. Sonner toasts on save success/error (uses `result.fieldErrors?.url?.[0] ?? .title?.[0] ?? result.error` to surface the first relevant message). Schema rules: title trimmed + non-empty, description/content/url/language nullable+optional, url validated as URL (using `z.url()` — the `z.string().url()` form is deprecated in Zod 4), tags array of trimmed non-empty strings. Installed `zod@4.4.1`. Vitest coverage: `src/lib/validations/items.test.ts` with 10 cases (title trim/required/whitespace-only, url valid/invalid/null, tag array, tag trimming, empty-after-trim rejection); the server action wrapper itself is left untested per the project's "avoid mocking Prisma; real-DB integration tests are out of scope" stance. Browser-verified end-to-end with the demo user: clicking a snippet card opens the drawer with full content, Edit flips to edit mode showing content+language fields, Cancel discards local edits and restores view, Save persisted a tag-array round-trip (added "edit-test" then reverted) with the Updated date flipping to today, link items show only the URL field (no content/language), invalid URL on a link is blocked at form level by HTML5 `type="url"` validation before reaching the server (Zod still enforces it on direct API calls). One TypeScript hint left in place: `React.FormEventHandler` shows a "deprecated" hint under React 19 typings but the build passes cleanly. Build verified (`next build`, 17 routes); `npm run test:run` 4 files / 32 tests pass (~200ms). Heads-up for next iteration: the action bar's Favorite/Pin/Delete are still local-only stubs, the type-specific renderers (real code editor, file/image preview, link unfurl) and collection-membership editing remain out of scope. Merged to main.

- **2026-04-30**: Delete Item (Drawer + Confirmation) - Replaced the item drawer's "coming soon" Delete stub with a working destructive flow. Added `deleteItem(itemId, userId): Promise<boolean>` to `src/lib/db/items.ts` — does an ownership pre-check (`prisma.item.findFirst({ where: { id, userId } })`) and returns false if the row doesn't belong to the caller, otherwise calls `prisma.item.delete`. No explicit cleanup of join rows needed: `ItemCollection` has `onDelete: Cascade` on both sides and `Item` ↔ `Tag` is an implicit M2M whose join rows Prisma drops automatically when either parent is deleted. Added `deleteItem(itemId): Promise<{ success: true } | { success: false, error: string }>` server action in `src/actions/items.ts` mirroring the `updateItem` pattern: `auth()` gate (401-style "Not authenticated" error), demo-user resolution via `getDemoUserId() ?? session.user.id` for parity with the dashboard's demo-user shortcut, calls the DB query, surfaces "Item not found" on the false branch, and returns "Failed to delete item" inside a try/catch so unexpected Prisma errors don't crash the client. Built a reusable `src/components/ui/confirm-dialog.tsx` on top of the existing in-repo `Modal` (instead of installing shadcn's `AlertDialog` — keeps it consistent with `ChangePasswordDialog` / `DeleteAccountDialog`, which use the same Modal under the hood); supports `title`, `description`, `confirmLabel`, `pendingLabel`, `cancelLabel`, `variant` (defaults to `destructive`), and a `pending` flag that disables both buttons while the request is in flight and swaps the confirm label to the pending copy. Wired the drawer in `src/components/items/ItemDrawer.tsx`: lifted `confirmingDelete` + `deleting` state and `handleConfirmDelete` into the top-level `ItemDrawer` component (rather than `DrawerContent`) so the dialog is rendered as a sibling of `<Sheet>` and survives the drawer closing on success; threaded a new `onDelete` prop into `DrawerContent` and pointed the existing destructive Delete `ActionButton` at it (replacing the `handleStub('Delete')` call, which is now removed). On confirm: server action runs → success path fires `toast.success('Item deleted')`, closes the confirm dialog, calls `onClose()` (the provider's drawer close) and `router.refresh()` so the dashboard's stats cards, sidebar type counts, and Recent/Pinned lists drop the deleted row without a hard navigation. Failure path keeps both the dialog and drawer open and fires `toast.error(result.error)`. Description copy in the confirm dialog uses curly quotes around the item title (`"<title>" will be permanently deleted. This action cannot be undone.`) with a fallback string for the (effectively impossible) case where `item` is null. Browser-verified end-to-end with the demo user: opening "Lucide Icons" via the Recent Items card and clicking Delete → confirm dialog appears with the right title in the copy → Cancel closes the confirm but keeps the drawer open → re-clicking Delete and confirming actually removes the row (verified via Neon: `SELECT COUNT(*) FROM "Item" WHERE id = '...'` returned 0), the drawer closes, dashboard "Total Items" dropped 18 → 17, sidebar Links count dropped 6 → 5, no console errors. No Vitest tests added: the new DB query and the server action are both Prisma-touching with no pure-logic branches worth mocking — same stance the prior `updateItem` action was left untested under, and `ConfirmDialog` is a presentational component (out of scope per coding-standards.md). Existing suite re-run as a sanity check (4 files / 32 tests pass). Build verified (`next build`, 17 routes, no TS errors). Heads-up: the verification consumed the seeded "Lucide Icons" link from the demo user — run `npx prisma db seed` if you want to restore seed state. Heads-up: drawer's Favorite/Pin are still local-only visual toggles; persisting them is the next obvious follow-up. Merged to main.

- **2026-05-01**: Item Create (Dialog + Type-Conditional Fields) - Added a new shadcn Dialog opened from the top bar's "New Item" button, replacing the previously inert button. Installed shadcn `dialog` via `npx shadcn add dialog` (pulled in `@radix-ui/react-dialog`); kept the existing in-repo `Modal`/`ConfirmDialog`/`Sheet` untouched since they each fit their own purpose. New schema in `src/lib/validations/items.ts`: `CREATE_ITEM_TYPES = ['snippet','prompt','command','note','link'] as const` (file/image excluded — Pro-only types live in a separate upload flow), `createItemSchema` uses `z.enum(CREATE_ITEM_TYPES)` for `typeName`, mirrors the `updateItem` field rules (title trim+required, description/content/url/language nullable+optional, url validated via `z.url()`, tag array of trimmed non-empty strings), and adds a `superRefine` that requires `url` when `typeName === 'link'`. Added a `createItem(userId, data)` query in `src/lib/db/items.ts` that looks up the type via `findFirst({ where: { name, OR: [{ isSystem: true }, { userId }] } })` (returns null when missing so the action can surface "Item type not found"), maps `typeName → contentType` via a small `TYPE_CONTENT_TYPE` record (text for snippet/prompt/command/note, url for link, file for file/image — included for forward compat even though the action's enum currently rejects file/image), creates the row with `tags.connectOrCreate`, and returns `getItemDetail(created.id, userId)` so the action can return the full ItemDetail. New `createItem` server action in `src/actions/items.ts` mirroring `updateItem` exactly: `auth()` gate, Zod `safeParse` with `z.flattenError` fieldErrors on failure, tag dedup via `Array.from(new Set(...))`, demo-user resolution via `getDemoUserId() ?? session.user.id`, returns `{ success, data | error, fieldErrors? }`. Built `src/components/items/CreateItemDialog.tsx` as a client component using shadcn's Dialog primitives (Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription) — overrode `DialogContent`'s `bg-background` with `bg-[hsl(217.2_32.6%_25%)]` and an explicit border, same workaround the in-repo Modal applies because this project doesn't wire `@theme` in Tailwind v4 so `bg-background` renders transparent against the overlay. UI: 5-button type selector grid (icon + label per type with the spec colors snippet `#3b82f6`, prompt `#8b5cf6`, command `#f97316`, note `#fde047`, link `#10b981`), `aria-pressed` for active state, conditional fields driven by `TYPES_WITH_CONTENT` / `TYPES_WITH_LANGUAGE` / `typeName === 'link'` so snippet/command show content + language, prompt/note show content, and link shows a required URL input (HTML5 `type="url"` + `required` for client-side, Zod `superRefine` for server-side). Tags as comma-separated string, parsed and trimmed before submission. Submit disabled until trimmed title is non-empty (and trimmed URL non-empty for link). Resets all form state when `open` flips false so reopening starts fresh; `onOpenChange={(o) => !o && onClose()}` so backdrop click and Escape both close cleanly. Sonner toasts on success ("Item created") and failure (uses `result.fieldErrors?.url?.[0] ?? .title?.[0] ?? result.error` to surface the most relevant message); on success calls `onClose()` and `router.refresh()` so the dashboard's stats cards, sidebar type counts, and Recent Items drop the new row in. Wired in `src/components/dashboard/DashboardShell.tsx`: added `createOpen` state, pointed the existing top-bar `<Button>New Item</Button>`'s `onClick` at `setCreateOpen(true)`, mounted `<CreateItemDialog open={createOpen} onClose={() => setCreateOpen(false)} />` next to the SidebarDrawer. Vitest coverage: `src/lib/validations/items.test.ts` gained 9 cases for `createItemSchema` (snippet happy path, link with valid url, link without url rejected, invalid url, unknown type rejected, empty title, title trim, tag trim, prompt+note no-url ok). Suite: 4 files / 41 tests pass (~240ms). Browser-verified end-to-end with the demo user: clicking "New Item" opens the dialog with snippet preselected; created a snippet (title/description/content/language/tags) → toast fires, dialog closes, row appears in Recent Items + Snippets count 4 → 5, DB row stored with `contentType:'text'`, both tags connected; switched the type selector to Link → URL field appears, content/language hidden; created a link with `https://example.com/test` → row stored with `contentType:'url'`, dashboard "Total Items" 17 → 19; no console errors. Cleaned up the two test rows via `npx prisma db execute --file` (Neon MCP is read-only in this project so the in-app delete drawer or a one-off SQL file is the only DB write path). One TypeScript hint preserved: `React.FormEventHandler` shows the same React-19 deprecation hint already present in `ItemDrawer.tsx`; build passes. Build verified (`next build`, 17 routes, no TS errors); `npm run lint` reports 3 errors all pre-existing on `main` (cascading-render rule against setState-in-effect in DashboardShell/ItemDrawer/sheet) — confirmed by stashing changes and re-running lint. Heads-up: dialog opens from any page that uses `DashboardShell` (so `/items/[type]` gets it for free). The current type set is hardcoded to system non-Pro types (file/image excluded); the file upload flow + Pro gating live in a separate piece of work. Merged to main.

- **2026-05-01**: Code Editor (Monaco) for Snippet + Command - Replaced the plain `<textarea>` / `<pre><code>` for snippet and command items with a Monaco-backed `CodeEditor` in both the item drawer (view + edit) and the create dialog. Note and prompt keep the existing `<textarea>`. Installed `@monaco-editor/react` (Monaco itself was pulled in as a peer/transitive dep). New `src/components/items/CodeEditor.tsx` (`'use client'`) wraps `Editor` with a custom `devstash-dark` theme defined on first mount via `monaco.editor.defineTheme` — base `vs-dark` with the in-repo `#161e2c` background and lighter slate scrollbar slider colors so the chrome blends with the drawer/dialog surfaces. Header strip on top of the editor renders the three macOS-style window dots (`#ff5f57` / `#febc2e` / `#28c840`) on the left, a small uppercase language pill (falls back to `plaintext` when language is null/empty), and a Copy button on the right that reads from `editorRef.current?.getValue() ?? value` (so the live edit-mode buffer is what gets copied), writes to `navigator.clipboard`, and swaps the icon to `Check` for 1.5s without firing a sonner toast (the drawer already has its own Copy action; avoiding a double toast). Height is fluid: `editor.onDidContentSizeChange` runs `Math.max(min, Math.min(getContentHeight(), max))` (defaults `min=120` / `max=400`) and feeds it into the `<Editor>`'s `height` prop, with `wordWrap: 'on'`, `minimap: { enabled: false }`, `folding: false`, `overviewRulerLanes: 0`, `guides.indentation: false` to keep the chrome quiet inside narrow drawer/dialog widths. `readOnly` mode also sets `domReadOnly: true`, `renderLineHighlight: 'none'`, and `contextmenu: false`; an empty readonly value renders a placeholder div instead of an empty editor so the view drawer reads "No content." cleanly. Wired into `src/components/items/ItemDrawer.tsx`: added `TYPES_WITH_CODE_EDITOR = new Set(['snippet','command'])`; `ContentPreview` now branches on that set and renders `<CodeEditor value={item.content} language={item.language} readOnly />` for snippet/command (other text types keep the existing `<pre><code>` render); `DrawerEdit` swaps the content `<textarea>` for `<CodeEditor value={content} onChange={setContent} language={language} />` for those same types — the edit-mode language input still drives Monaco's syntax highlighting live as it's typed. Wired into `src/components/items/CreateItemDialog.tsx` the same way: identical `TYPES_WITH_CODE_EDITOR` set + `useCodeEditor` branch in the content `Field`. Keeping the conditional inside the existing `Field` wrapper preserves the field label + spacing without duplicating layout. Spec callout about a custom CSS scrollbar in `globals.css` was satisfied entirely through Monaco's theme `colors` (`scrollbarSlider.background` etc.) plus narrowed `scrollbar.{vertical,horizontal}ScrollbarSize: 8` — no `globals.css` changes needed. No new server actions / lib code, so no Vitest cases added; `npm run test:run` re-run as a sanity check (4 files / 41 tests pass). Build passes (`next build`, 17 routes, no TS errors); `npm run lint` reports the same 3 pre-existing errors on `main` (cascading-render rule against setState-in-effect in DashboardShell/ItemDrawer/sheet) — none from this change. Heads-up: full browser verification was blocked by a stale Playwright MCP `SingletonLock` in `~/Library/Caches/ms-playwright/mcp-chrome-7a5e720`, so the editor was not visually verified end-to-end this session — dev server + build + tests are green, and the bundle is reachable at `/items/snippets` (HTTP 200). Heads-up for next iteration: the user's follow-up ask "type-specific Add button on each /items/[type] page that preselects the type in the New Item dialog" is intentionally scoped to a separate feature branch (separate concern from the editor surface). Merged to main.

- **2026-05-01**: Type-Specific Add Button on Items List - Wired a per-type Add button into the `/items/[type]` page header that opens the existing `CreateItemDialog` with the matching type preselected, addressing the follow-up ask flagged in the prior Code Editor (Monaco) note. Refactored the dialog plumbing so a single dialog instance is shared between the dashboard top bar and the items pages instead of duplicating it: new `src/components/items/CreateItemContext.tsx` exports `CreateItemProvider` (mounts the dialog once at the shell level, owns `{ open: boolean; type?: CreateItemType }` state) and a `useCreateItem()` hook returning `{ open(type?) }`. The provider is wrapped around the body of `src/components/dashboard/DashboardShell.tsx` (replacing its prior local `createOpen` useState plus inline `<CreateItemDialog />` mount), so the items page automatically sits inside it and any client child can trigger the dialog. Two tiny client buttons consume the hook: `src/components/items/TopBarNewItemButton.tsx` (calls `open()` with no arg → snippet default, replaces the inline top-bar `<Button>New Item</Button>` in DashboardShell) and `src/components/items/TypeAddButton.tsx` (`open(type)`, "Add <Type>" label, `<Plus />` icon styled with the type's brand color via `style={{ color: meta.color }}`). `CreateItemDialog` gained an optional `defaultType?: CreateItemType` prop; replaced the prior `if (!open) reset to 'snippet'` effect with a unified `useEffect([open, initialType])` that sets `typeName` to `defaultType ?? 'snippet'` on both open transitions and close-resets — so a fresh dialog open with `type='note'` lands on Note immediately, and reopening from the top bar goes back to Snippet without leaking state across opens. Extracted the type label/icon/color metadata (previously a local `TYPE_META` const inside `CreateItemDialog`) into `src/lib/item-type-meta.ts` as `CREATE_TYPE_META: Record<CreateItemType, { label, icon, color }>` so both `CreateItemDialog` and `TypeAddButton` source the spec colors (snippet `#3b82f6`, prompt `#8b5cf6`, command `#f97316`, note `#fde047`, link `#10b981`) from one place. The items page (`src/app/items/[type]/page.tsx`) wraps the existing icon+title+count block in a `flex items-center justify-between` row and renders `<TypeAddButton type={type.name} />` on the right side gated by `(CREATE_ITEM_TYPES as readonly string[]).includes(type.name)` — so `/items/snippets|prompts|commands|notes|links` all get the button while `/items/files` and `/items/images` correctly do not (Pro-gated upload flow lives elsewhere). Out of scope: the file/image upload flow itself, and migrating the top-bar "New Collection" button to the same pattern. No DB/schema changes, no server actions added — the existing `createItem` action covers everything, and no Vitest cases needed under our scope (the only new lib file is a pure typed-constant lookup, and `CREATE_ITEM_TYPES` is already covered by the 9 `createItemSchema` cases from the Item Create feature). `npm run test:run` 4 files / 41 tests pass; `next build` clean (17 routes, no TS errors); `npm run lint` reports the same 3 pre-existing errors on `main` (DashboardShell/ItemDrawer/sheet set-state-in-effect rule) — none from this change. `curl --noproxy '*'` smoke test: `/items/snippets|notes|files` → 200, `/items/bogus` → 404. Heads-up: full browser verification was blocked again by the same stale Playwright MCP `SingletonLock` in `~/Library/Caches/ms-playwright/mcp-chrome-7a5e720` (Chrome PID 19763 still owns it from the prior session) — the dialog preselect and per-type buttons were not visually verified end-to-end this session, but the build/tests/curl are all green. Merged to main.

- **2026-05-02**: Markdown Editor for Note + Prompt - Replaced the plain `<textarea>` for note and prompt items with a tabbed Write/Preview `MarkdownEditor` in the create dialog and item drawer (snippet/command keep `CodeEditor`; link has no content field). Installed `react-markdown@^10` + `remark-gfm@^4`. New `src/components/items/MarkdownEditor.tsx` (`'use client'`) renders a header strip styled to match `CodeEditor`'s chrome (`bg-[#2d2d2d]`, container `bg-[#1e1e1e]`) with Write/Preview tabs (`aria-pressed` for active state) and a Copy button on the right that mirrors `CodeEditor`'s pattern (clipboard write + 1.5s `Check` icon swap, no sonner toast — drawer-level Copy action already toasts). Initial `tab` state is `readOnly ? 'preview' : 'write'`; a `useEffect([readOnly])` forces the preview tab whenever readonly flips on. Write mode renders a `<textarea>` with monospace font + auto-resize: `useEffect([tab, value, readOnly, minHeight, maxHeight])` reads `scrollHeight` and clamps to `Math.max(min, Math.min(scrollHeight, max))`. Preview mode renders `<ReactMarkdown remarkPlugins={[remarkGfm]}>` inside a `.markdown-preview` div, with a placeholder shown when `readOnly && !value` ("Nothing to preview."). Default heights `min=240` / `max=400` — bumped from initial `160/400` after user feedback that the editor was opening too short for typing prose. Wired into both surfaces with a new shared `TYPES_WITH_MARKDOWN_EDITOR = new Set(['note', 'prompt'])` and a 3-way conditional inside the existing content `Field`: `useCodeEditor ? CodeEditor : useMarkdownEditor ? MarkdownEditor : <textarea>`. `src/components/items/CreateItemDialog.tsx` consumes the branch in its content `Field` (snippet/prompt/command/note still pass through `TYPES_WITH_CONTENT`; link's URL field and snippet/command's language field stay untouched). `src/components/items/ItemDrawer.tsx` consumes it in two spots: `DrawerEdit` (edit mode) for note/prompt content, and `ContentPreview` (view mode, readonly) which now branches `TYPES_WITH_CODE_EDITOR ? CodeEditor : TYPES_WITH_MARKDOWN_EDITOR ? MarkdownEditor readOnly : <pre><code>`. Dark-theme markdown styling lives under a single `.markdown-preview` class added to `src/app/globals.css` covering: h1–h6 with distinct sizes (h1 1.5rem, h2 1.25rem with bottom-borders) + weights, inline `code` (subtle bg + amber `#fbbf24` text), `pre` blocks (`#161e2c` bg + bordered + scrollable + monospace inheriting), `pre code` color reset, `ul`/`ol` with proper padding-left 1.5rem and disc/decimal markers (`::marker` colored slate), GFM task-list `input[type="checkbox"]` accent, blockquote with 3px `#60a5fa` left border + faint bg + slate text, `hr`, `table` (display: block + overflow-x: auto for narrow drawer widths) with `th` header bg and zebra `tr:nth-child(2n)`, `img` max-width, `strong`/`em`/`del`. Initial implementation put the rules at the top level of `globals.css` and Tailwind v4 + Turbopack silently dropped them from the bundled CSS (verified: `grep -c markdown-preview` on the dev CSS bundle returned 0); root cause is that Tailwind v4's CSS pipeline preserves rules inside `@layer` directives but strips unlayered top-level rules, while the existing `:root` / `.dark` blocks were preserved because they sat inside `@layer base`. Fix was to wrap the entire `.markdown-preview` block in `@layer components` — after save, HMR rebuilt and the rules now appear 35× in the dev bundle and ship in the production CSS chunk. The `@tailwindcss/typography` `prose` plugin is intentionally NOT used: spec called for a custom class for reliable dark-mode styling, and the layer-wrapped custom CSS works fine. No DB/schema changes, no server actions, no new utility/library code — out of unit-test scope per `coding-standards.md` (client component + CSS only). `npm run test:run` 4 files / 41 tests pass (existing suite unchanged); `next build` clean (17 routes, no TS errors). Browser-verified end-to-end with the demo user via the seeded "Test Node with Markdown Editor" note: drawer view mode rendered `# Heading` as a styled h1 and `- item1 / - item2` as a bulleted list with proper indentation; edit mode flipped to Write tab with the textarea showing the raw markdown source; CreateItemDialog with Note preselected showed the markdown editor in place of the old textarea, Preview tab rendered the input live. Heads-up for next iteration: the `MarkdownEditor` is currently a controlled-only component — no debounced preview, no syntax highlighting in Write mode, no slash-command toolbar; those are all out of scope for this pass. Merged to main.

- **2026-05-04**: File Upload with Cloudflare R2 - Added file and image item types end-to-end with R2-backed uploads, server-side validation, an auth-gated download proxy, and orphan cleanup on item delete. Installed `@aws-sdk/client-s3`. New `src/lib/r2.ts` lazy-builds an S3-compatible client only when all four env vars are present (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME` — note: matched the user's existing `.env` convention rather than my initial `R2_BUCKET`; renamed mid-feature after a 503 from the live upload route), endpoint hard-coded to `https://${accountId}.r2.cloudflarestorage.com`, region `'auto'`, exposing `isR2Configured` / `putObject` / `getObject` / `deleteObject` plus a `GetObjectResult` that wraps the SDK body via `transformToWebStream()` so the proxy can `return new Response(stream)` without buffering. New `src/lib/file-constraints.ts` is the single source of truth for size + MIME + extension allowlists: 5 MB / `.png|.jpg|.jpeg|.gif|.webp|.svg` for images, 10 MB / `.pdf|.txt|.md|.json|.yaml|.yml|.xml|.csv|.toml|.ini` for files; `validateUpload(kind, name, size, mime)` accepts when EITHER the MIME OR the extension matches (handles browsers that don't set MIME for `.svg`/`.yml`/`.toml`/`.ini` — `.ini` also accepts `text/plain`/`application/octet-stream`/empty MIME); also exports `formatBytes`, `getExtension`, `sanitizeFileName` (replaces unsafe chars with `_`, strips leading/trailing underscores, falls back to `'file'` for empty input, caps at 120 chars). POST `/api/upload` (`runtime: 'nodejs'` so we have `crypto.getRandomValues` + Buffer): `auth()` gate, 503 short-circuit when R2 isn't configured, parses multipart with `req.formData()`, expects `kind` (`'file'`|`'image'`) + `file` (Blob/File), runs the same `validateUpload` server-side, then writes to R2 at `users/<ownerId>/<kind>/<24-hex-id>-<sanitized-name>` with the original Content-Type. Demo-user resolution mirrors the rest of the app (`getDemoUserId() ?? session.user.id`). Returns 201 `{ key, url, fileName, fileSize, contentType }` where `url = '/api/files/<key>'`. `randomKey()` uses `crypto.getRandomValues(Uint8Array(12))` → 24-hex string for collision-resistant suffixes. GET `/api/files/[...key]/route.ts` is the auth-gated download proxy (catch-all so the embedded `/` in keys doesn't 404): `auth()` → 401, `isR2Configured()` → 503, joins the `key[]` segments and `decodeURIComponent`s each one, then does ownership via DB lookup (`prisma.item.findFirst({ where: { userId, fileUrl: '/api/files/<key>' } })`) — the matching item row IS the authorization check, no separate ACL needed. Streams the R2 body back with the stored `Content-Type`, `Content-Length` if known, `Cache-Control: private, max-age=300`, and `Content-Disposition` switching between `inline` (default — for image previews) and `attachment; filename="<original>"` when `?download=1` is in the query. Schema and DB changes in `src/lib/validations/items.ts` and `src/lib/db/items.ts`: `CREATE_ITEM_TYPES` grew from 5 to 7 (adds `file`, `image`); `createItemSchema` adds optional `fileUrl/fileName/fileSize` and a `superRefine` that requires `fileUrl` when `typeName` is `file` or `image` (mirrors the existing `link` requires-`url` pattern). `createItem` query persists those three columns; the existing `TYPE_CONTENT_TYPE` map already covered file→`'file'`, image→`'file'`, link→`'url'` for forward compat from the prior Item Create feature. `deleteItem` does a best-effort R2 cleanup: pre-fetches `{ id, fileUrl }`, deletes the Prisma row first, then if `fileUrl` starts with `/api/files/` and R2 is configured, calls `deleteObject(key)` inside a try/catch (R2 failure is logged but doesn't fail the user-facing delete — the DB row is the source of truth). `src/actions/items.ts::createItem` threads the new fields through. `CREATE_TYPE_META` in `src/lib/item-type-meta.ts` adds the file (`#6b7280`) and image (`#ec4899`) entries so the dialog's type selector and per-type Add button on `/items/files`|`/items/images` (which now light up automatically because `CREATE_ITEM_TYPES` includes them) source from one place. New `src/components/items/FileUpload.tsx` is the drag-drop / click-to-pick widget with upload deferred until form submit — first iteration uploaded on file pick, but cancelling the dialog left an orphan in R2. Refactored: `value: PendingFile | null` (just the local `File` + an optional `previewUrl` for image thumbs), parent owns the eventual upload, `progress?: number | null` prop drives an inline progress bar inside the preview chip during upload. `validateUpload` runs client-side on pick (early bail with inline error message) and again server-side. Preview chip uses `break-all` on the filename (not `truncate`) per UX feedback so long names wrap mid-string instead of ellipsing; image thumbs use `shrink-0 h-16 w-16 object-cover`; X button is `shrink-0` and disabled during upload; object-URLs are revoked on `clearFile`/replace/unmount. The XHR upload helper is exported as `uploadFile(file, kind, onProgress)` and called from `CreateItemDialog`'s `handleSubmit`: upload first → on success thread `fileUrl/fileName/fileSize` into `createItem` → on failure surface the error message via toast and keep the dialog open. Submit button copy switches between `Create` → `Uploading 42%` (live percent from XHR `upload.onprogress`) → `Creating…`. Type selector grid widened from `grid-cols-5` to `grid-cols-4 sm:grid-cols-7` to fit the two new types. `src/components/items/ItemDrawer.tsx` got a Download anchor in the action bar (gated on `contentType === 'file'` && `fileUrl`) that links to `${fileUrl}?download=1` so the proxy sets `Content-Disposition: attachment`. `ContentPreview` now branches: image items render an `<img src={fileUrl}>` capped at `max-h-96 w-full object-contain` above the file metadata card (uses the proxy's default `inline` disposition); other file types just show the metadata card. File card name uses `break-all` so long filenames wrap inside the drawer column. Tests added under `coding-standards.md` scope: `src/lib/file-constraints.test.ts` (22 cases covering every branch in `validateUpload` for both kinds, plus `getExtension`/`formatBytes`/`sanitizeFileName`), `src/lib/r2.test.ts` (3 cases for `isR2Configured()` env-parsing — same env-restoring `beforeEach`/`afterEach` pattern as `features.test.ts`, exercises the all-set / each-individual-missing / all-unset matrix), and 4 new `createItemSchema` cases (file with `fileUrl` ✓, file without ✗ with `fieldErrors.fileUrl` assertion, image without ✗, plus the existing "unknown type rejected" case updated since `'file'` is now a valid type — uses `'whatever'` instead). `r2.ts` SDK calls and the route handlers stay untested per the project's "avoid mocking external services" stance. Suite now 6 files / 66 tests pass (~370ms). `next build` clean (19 routes — adds `/api/upload` and `/api/files/[...key]`); `npm run lint` reports the same 4 pre-existing errors on `main` (DashboardShell/ItemDrawer/MarkdownEditor/sheet `set-state-in-effect` rule, confirmed by `git stash` + re-run) — none from this change. `.env.example` (gitignored locally) documents `R2_ACCOUNT_ID` / `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_BUCKET_NAME` with a "503 when unset" note. Heads-up: the user's existing `.env` also has `R2_PUBLIC_URL` (unused — the proxy approach intentionally doesn't link to the public CDN URL so all downloads stay auth-gated). Edge case left in: if R2 upload succeeds but the subsequent `createItem` action fails (very unlikely since both run under the same session/auth), the R2 object is orphaned — would need a `deleteObject` cleanup-on-failure pass to close that gap. Other follow-ups not addressed: editing a file/image item's attached file (drawer edit mode currently doesn't expose the FileUpload), per-user storage quota, and Pro gating (file/image are intentionally available to all users during dev per the "all features unlocked in dev" rule). Merged to main.

- **2026-05-04**: Image Gallery View - Replaced the standard `ItemCard` on `/items/images` with a new gallery-style `ImageThumbnailCard` so image items render as visual thumbnails instead of metadata cards. Other item-type pages (`/items/snippets|prompts|commands|notes|links|files`) keep `ItemCard` untouched. New `src/components/items/ImageThumbnailCard.tsx` (server component, no `'use client'`): outer container is `group h-full overflow-hidden rounded-xl border bg-card transition-colors hover:bg-accent/50`; thumbnail wrapper is `relative aspect-video w-full overflow-hidden bg-muted` with the `<img>` rendered as `h-full w-full object-cover transition-transform duration-300 ease-out group-hover:scale-105` — that's the spec's 5%/300ms zoom, applied via Tailwind utilities so the `group-hover` reaches the inner image without state plumbing. `loading="lazy"` on the thumbnail to defer offscreen network requests; `alt={fileName || title}` so screen readers get a meaningful label even when the user didn't customize the title. When `fileUrl` is null (defensive — `createItemSchema` requires it for images, so this is only reachable for legacy/orphaned rows), renders a centered `lucide-react` `Image` icon over the muted background instead of a broken `<img>`. Pin/Favorite indicators move from the bottom-of-card chip layout used by `ItemCard` to a corner overlay: `absolute right-2 top-2 flex items-center gap-1 rounded-md bg-black/55 px-1.5 py-1 backdrop-blur-sm`, white pin and yellow-400 star — the dark blur ensures contrast on bright thumbnails. Title rendered as a single `truncate text-sm font-medium` line in a 12px-padded footer; description and tags intentionally dropped because the gallery view prioritizes the visual. Linted around `<img>` instead of `next/image`: thumbnails come from `/api/files/<key>` (auth-gated proxy added in the prior File Upload feature), so they're not statically known at build time and `next/image`'s remote-pattern config + size-attribute requirements would add friction without buying anything for these private user uploads — kept the same `// eslint-disable-next-line @next/next/no-img-element` pattern already used in `ItemDrawer.tsx`'s image preview. Extended `ItemListItem` in `src/lib/db/items.ts` with `fileUrl: string | null` and `fileName: string | null` and threaded those through the existing `getItemsByType` Prisma `select`/map — both fields were already on `Item` from the file-upload feature, so no migration. The other item-type pages don't read these fields, so the schema-widening is harmless. The `/items/[type]` page (`src/app/items/[type]/page.tsx`) gained one branch in the grid render: `type.name === 'image'` paths through `<ImageThumbnailCard ... />` (still wrapped in `<ItemTrigger itemId={item.id}>` so clicks still open the existing `ItemDrawer` — drawer integration unchanged), the else branch keeps the existing `<ItemCard>`. Grid classes (`grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3`), header (icon tile + heading + count), empty-state card, and the `TypeAddButton` ("Add Image") all stay exactly as-is — empty state and Add button correctly bypass the gallery branch. No DB writes, no schema migration, no server action work, no new pure utilities — out of unit-test scope per `coding-standards.md` (presentational client/server components and a typed-field addition only). Browser verification was again blocked because no Playwright MCP is wired up in this session, so I did SSR-fetch verification with `curl --noproxy '*'`: `/items/images` ships markup containing `aspect-video`, `object-cover`, and `group-hover:scale-105` (3/3 expected classes), with a real `<img src="/api/files/users/.../image/...">` URL for the seeded image item — that confirms both the conditional render path and that the proxy URL is being passed through correctly. `/items/snippets` returns 0 occurrences of any of the gallery classes (existing `ItemCard` path preserved). `npm run test:run` 6 files / 66 tests pass (existing suite unchanged in ~380ms); `next build` clean (19 routes, no TS errors). Heads-up: thumbnails load through the auth-gated proxy, so a logged-out user would see broken-image placeholders if they ever hit this page — moot in practice because the page is reached via the sidebar which itself requires a session, and `proxy.ts` already gates `/dashboard/*`; if `/items/*` ever needs the same proxy redirect, that's a separate one-line addition. Heads-up for next iteration: hover zoom is CSS-only (no preloaded high-res variant), no lightbox/full-size modal yet (clicking the thumbnail opens the existing drawer with the inline `<img>` capped at `max-h-96`), and there's no masonry layout (uniform 16:9 cells means tall portrait images get cropped — same tradeoff the spec called out with "edges may be cropped"). Merged to main.

- **2026-05-04**: File List View - Replaced the grid card layout on `/items/files` with a single-column Drive/Dropbox-style row list, mirroring the prior Image Gallery View branching pattern. Other item-type pages (`/items/snippets|prompts|commands|notes|links|images`) keep `ItemCard` / `ImageThumbnailCard` untouched. New `src/components/items/FileListRow.tsx` (`'use client'` — needed because the row owns the drawer-open click handler directly, and a download `<a>` can't legally nest inside the existing `ItemTrigger` `<button>` wrapper used by the other branches): outer container is a `<div role="button" tabIndex={0}>` with `onClick={openItem(id)}` and an `onKeyDown` that handles Enter + Space (calling `e.preventDefault()` so Space doesn't scroll), styled `flex cursor-pointer items-center gap-3 rounded-lg border bg-card p-3 transition-colors hover:bg-accent/50 focus:outline-none focus-visible:ring-2 focus-visible:ring-ring`. Left column: 40×40 muted square hosting a lucide icon picked by extension via a local `EXTENSION_ICONS: Record<string, LucideIcon>` map (`.pdf|.txt|.md` → `FileText`, `.json|.yaml|.yml|.xml|.toml|.ini` → `FileCode`, `.csv` → `FileSpreadsheet`, default `File`); icon lookup uses the existing `getExtension(name)` helper from `src/lib/file-constraints.ts` and a direct `EXTENSION_ICONS[ext] || FileIcon` lookup (mirrors `ItemCard`'s `iconMap[typeIcon] || DefaultIcon` pattern — initial implementation wrapped this in an `iconForFileName()` helper which tripped the `react/jsx-no-undef`-style `Cannot create components during render` ESLint rule, so inlined the lookup). Center column (`min-w-0 flex-1`): `displayName = fileName || title` rendered as `truncate text-sm font-medium` with `Pin` / `Star` indicators inline (matches `ItemCard`'s "icons next to title" treatment for consistency), plus a mobile-only `<p class="sm:hidden text-xs text-muted-foreground">` under the name showing `${size} · ${date}` so the metadata is reachable without horizontal scroll on narrow screens. Right side (desktop only — `hidden ... sm:block`): two fixed-width columns for size (`w-20 text-right`) and date (`w-28 text-right`) so rows align Drive-style. Date format includes year (`'short' month, numeric day, numeric year`) since file lists tend to span longer time windows than the Recent Items dashboard widget. Download button is an `<a>` (not a `<button>`) so it inherits real anchor semantics + middle-click + right-click "Save link as": `href=${fileUrl}?download=1` (the existing auth-gated proxy at `/api/files/[...key]` flips `Content-Disposition: attachment` when `?download=1` is present), `download={fileName ?? undefined}` to suggest the original filename, and `onClick / onKeyDown` both call `e.stopPropagation()` so the click doesn't bubble to the row's drawer-open handler. When `fileUrl` is null (defensive — `createItemSchema` requires it for files, only reachable for legacy/orphaned rows), renders a disabled `<span>` with the same icon at half opacity. Extended `ItemListItem` in `src/lib/db/items.ts` with `fileSize: number | null` (was already on `ItemDetail` and the `Item` table from the File Upload feature, just not threaded through the list query) and added `fileSize: item.fileSize` to `getItemsByType`'s map; `formatBytes()` from `src/lib/file-constraints.ts` is the formatter. The other item-type pages don't read this field, so the widening is harmless. `/items/[type]/page.tsx` gained one new branch between `type.name === 'image'` and the default `<ItemCard>` grid: `type.name === 'file'` renders `<div class="flex flex-col gap-2">` of `FileListRow`s (no `ItemTrigger` wrap — the row owns its own click handler) and bypasses the `grid grid-cols-1 ... lg:grid-cols-3` layout that the other types share. Empty state and `TypeAddButton` for `/items/files` are unchanged because they live before the branch. No server actions or new pure utilities, so out of unit-test scope per `coding-standards.md`; `npm run test:run` re-run as a sanity check (6 files / 66 tests pass, ~340ms). `next build` clean (19 routes, no TS errors); `npm run lint` reports the same 4 pre-existing errors on `main` (DashboardShell/ItemDrawer/MarkdownEditor/sheet `set-state-in-effect`) — none from this change (initial implementation produced one extra error from the `iconForFileName()` helper, fixed by inlining the lookup). SSR verified via `curl --noproxy '*'` against the dev server: `/items/files` (HTTP 200) ships 1 row of the new markup with the seeded `Huilong_Wang_Resume.pdf`, the `?download=1` anchor URL, and the responsive `sm:hidden` / `hidden w-20` / `hidden w-28` classes; `/items/snippets` still has the `borderLeftWidth` ItemCard markup; `/items/images` still has the `aspect-video` gallery markup — confirms the conditional dispatch. Heads-up: still no item-list pagination — fine for now since the demo user has 1 file, but a heavy-uploader account would pay the SSR cost of `findMany` returning everything in `getItemsByType`. Heads-up for next iteration: download button is icon-only with no per-row checkboxes / multi-select, no inline rename, and no sort controls (Drive surfaces a "Name | Modified | Size" column header that's clickable for sort) — the current header above the list is a single block of metadata, not sortable columns. Merged to main.

- **2026-05-05**: Quick Copy Icon on Item Cards - Added a small copy button to the bottom-right corner of `/items/[type]` listing cards so users can grab the contents without opening the drawer. New `src/components/items/CopyButton.tsx` (`'use client'`) renders a 28×28 icon button using lucide `Copy` (swapped to emerald-tinted `Check` for 1.5s on success), writes via `navigator.clipboard.writeText`, fires `sonner` toasts ('Copied to clipboard' / 'Copy failed'), and crucially calls `e.stopPropagation()` + `e.preventDefault()` on click + `e.stopPropagation()` on keydown so the surrounding `ItemTrigger` doesn't open the drawer behind the toast. Accepts an optional `label` (defaults to 'Copy') used for both `aria-label` and `title`. `src/components/items/ItemCard.tsx` gained `content: string | null` and `url: string | null` props; `copyText = (content ?? url ?? '').trim()` selects what to copy (`content` for snippet/prompt/command/note, `url` for link) and `showCopy = copyText.length > 0` hides the button when there's nothing meaningful to copy. Replaced the prior conditional tags-only row with a unified `mt-auto flex items-end justify-between gap-2` row that always sits at the bottom whenever tags or copy is present; tags wrap on the left, the `CopyButton` is corner-pinned on the right via `justify-between` (so the button position is consistent regardless of tag count). Empty cards (no tags + nothing copyable) skip the bottom row entirely. `src/components/items/ItemTrigger.tsx` switched from a wrapping `<button>` to `<div role="button" tabIndex={0}>` + `onClick` + `onKeyDown` (Enter/Space → `e.preventDefault()` + `openItem(itemId)`) — same accessibility pattern `FileListRow` uses — because nesting a real `<button>` inside another `<button>` is invalid HTML5 and trips React's `validateDOMNesting`; the role+tabIndex pattern keeps the drawer-open behavior on click and keyboard while letting the inner copy button be a real `<button>`. ItemTrigger is also used by the dashboard's `PinnedItem` / `RecentItem` renders, so the markup change propagates there for free without functional impact (those components don't render a copy button themselves — out of scope per the spec, easy follow-up if requested). Extended `ItemListItem` in `src/lib/db/items.ts` with `content: string | null` and `url: string | null` and added them to the `getItemsByType` map (both fields were already on `Item` from the original schema, so no migration). `src/app/items/[type]/page.tsx` threads the new props into the non-image, non-file `<ItemCard>` branch. `ImageThumbnailCard`, `FileListRow`, dashboard `PinnedItem`, and dashboard `RecentItem` are intentionally untouched — image/file cards have no plain-text payload (image already has the inline `<img>`, file rows already own that corner with a download anchor), and dashboard cards were left out of the spec's scope. No new server actions or pure utility/library code added (the only logic in `CopyButton` is component behavior — clipboard + state timer — which is out of unit-test scope per `coding-standards.md`'s "no component tests" rule), so no new Vitest cases warranted; existing suite re-run as a sanity check (6 files / 66 tests pass in ~370ms). `next build` clean (19 routes, no TS errors); `npm run lint` reports the same 4 pre-existing errors on `main` (DashboardShell/ItemDrawer/MarkdownEditor/sheet `set-state-in-effect`) — none from this change. Browser verification done manually by the user. Heads-up for next iteration: dashboard `PinnedItem` / `RecentItem` cards don't have the copy button yet (out of scope per the spec); if added, they'd need the same `content`/`url` plumbing into `DashboardItem` and the `getPinnedItems` / `getRecentItems` queries. Merged to main.
